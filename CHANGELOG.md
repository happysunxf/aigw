# AI 网关持续深挖 · CHANGELOG

> 自动化 cron 任务产出记录。每 30 分钟一次，主题按 `local hour % 7` 轮换。
> 报告文件存放在 `reports/`，命名格式 `YYYY-MM-DD-HHMM-aigw-<topic>.md`。

## 2026-06-06

- 18:58 · `guardrails` · round 6 — **供应链安全（CVE-2026-45758 24 天未收尾）/ PII 多国识别器 / 多模态 red-team 评估 / saved cloud token 隔离** 专题。聚焦过去 14d guardrails 三栈 + promptfoo 同步硬化集锦：(1) **guardrails-ai v0.10.2 (6/4 19:37 UTC)** [GHSA-xmpw-2vmm-p4p6 / CVE-2026-45758](https://github.com/guardrails-ai/guardrails/security/advisories/GHSA-xmpw-2vmm-p4p6) — 5/11 18:00 PT 投毒 → 5/11 20:00 PT 识别（~2h）→ 5/12 advisory → 5/13 14:00 PT 整 org 轮换所有 Snowglobe/Hub key → 6/4 v0.10.2（7 项 security advisory update + litellm pin #1484 + trusted publishing #1493）→ **6/6 issue #1473 仍 open**，PyPI quarantine **持续 26 天**未解除。**同窗口同 attacker**：[TanStack CVE-2026-45321 / GHSA-g7cv-rxg3-hmpx](https://github.com/TanStack/router/security/advisories/GHSA-g7cv-rxg3-hmpx)（5/11 22:40 UTC, 42 个 @tanstack/* 包 credential exfil）—— guardrails-ai SECURITY_ADVISORY.md 显式 cross-reference。OSS 供应链攻击 24+ 天 quarantine 对生产是 deal-breaker，**所有 AIGW 必须有"PyPI quarantined → git tag fallback" runbook**。(2) **presidio PR #1970 Unified Analyzer Configuration** (SharonHart, 5/17, 30 files / **+1179/-146**) — 合并 `default_analyzer.yaml` + `default.yaml` + `default_recognizers.yaml` 三个文件到单一 `analyzer.yaml`，旧文件保留 deprecation banner = **消除"两处改一处忘" P0 治理洞**。配套：[#2000](https://github.com/microsoft/presidio/pull/2000) `countries=["US","DE"]` 过滤参数（**`countries=[]` 只留 locale-agnostic**）= locale-aware 部署"一行 API" / [#2025](https://github.com/microsoft/presidio/pull/2025) **修复 `validate()` 调 lambda 破坏 stateful 状态** —— `Custom.validate()` 之前探针 `new_val("PII")` 会真实执行 lambda，对**有状态 lambda（anonymizer 把 token→原值映射存 map 做 de-anonymize）插入 `{"TOKEN_1": "PII"}` 假条目，让所有 token 计数器 +1**；修法 `validate()` 只检 `callable()`，type contract 移到 `operate()` 真实数据路径，**3 个新 test 含 `test_stateful_token_map_not_corrupted_by_validate`**；新识别器：[#2038 PH mobile](https://github.com/microsoft/presidio/pull/2038) / [#2006 TR phone](https://github.com/microsoft/presidio/pull/2006) / [#2011 ES passport](https://github.com/microsoft/presidio/pull/2011) / [#2014 configurable Phonenumber entity](https://github.com/microsoft/presidio/pull/2014)。(3) **garak v0.15.1 (6/5 17:24 UTC)** — [PR #1504 ProPILE PII 探针](https://github.com/NVIDIA/garak/pull/1504)（arXiv:2307.01881, 默认 `active=False` + `tier=INFORMATIONAL`，4 个 probe = Twin/Triplet/Quadruplet/Unstructured，**已知 1/2/3 条 PII 推断第 N 条**），与 `leakreplay` "训练集逐字回放"互补；[PR #1807 cas digest](https://github.com/NVIDIA/garak/pull/1807) `eval.intents` (per-eval) map `{passed, total_evaluated, nones}` + 顶层加性字段 `digest.technique_intent_matrix`（probe `demon:*` tag 投出 technique × intent 交叉表，**与 `reporting.taxonomy` 解耦**）；配套 [PR #1839 js deps](https://github.com/NVIDIA/garak/pull/1839) / [#1781 data_path path traversal 修](https://github.com/NVIDIA/garak/pull/1781) / [#1732 删 maxrecall evaluator](https://github.com/NVIDIA/garak/pull/1732) 强制 `z_rating` defcon / [#1794 inline score_to_defcon](https://github.com/NVIDIA/garak/pull/1794) / [#1791 config dedup regression](https://github.com/NVIDIA/garak/pull/1791) / [#1738 keep packagehallucination entries with invalid dates](https://github.com/NVIDIA/garak/pull/1738)。(4) **promptfoo 0.121.15 (6/5 14:21 UTC)** —— **必看 PR #9597 `fix(redteam): restrict remote auth to cloud host`**：saved Promptfoo Cloud bearer token 限制在 origin 与 `cloudConfig.getApiHost()`（含 on-prem `:8443`）匹配的请求，**token 永远不会泄漏到 custom `PROMPTFOO_REMOTE_GENERATION_URL` 或 `PROMPTFOO_UNALIGNED_INFERENCE_ENDPOINT`**，`isPromptfooCloudApiHost` 比 `URL.origin` 与配置 host（同 vs 异 port / http vs https / unparseable host fail-closed），**Do not override caller-supplied `Authorization`** 修了一个 re-validating/rotating key 把旧 saved token 注入已登录 on-prem host 的回归（**未验证 key 也会被保存**），**230 test 覆盖**。其他：[#9617 multimodal grading](https://github.com/promptfoo/promptfoo/pull/9617) `providerResponse.images` 传 grader，**公开 HTTP(S) image URL 自动 hydrate 到 data URL**，**私有 / local URL 默认跳过 + `PROMPTFOO_ALLOW_GRADING_IMAGE_PRIVATE_URLS=true` 显式 opt-in**，result metadata 只存 text + image count **不存 image bytes**；[PR #9586 A2A provider first-class](https://github.com/promptfoo/promptfoo/pull/9586)（HTTP+JSON v1, Agent Card discovery / message:send polling / message:stream SSE / task polling，**与 MCP provider 同框架 + Agent Card skills/capabilities 喂 redteam 生成 context**）；[PR #9453 agent-rubric grader](https://github.com/promptfoo/promptfoo/pull/9453) Codex SDK safe default；[PR #9609 CSV formula injection escape](https://github.com/promptfoo/promptfoo/pull/9609) 防 CWE-1236；[PR #9626 inverse-when-not-JSON](https://github.com/promptfoo/promptfoo/pull/9626) 修 `not-equals` 路径 `pass = false` hard-code 忽略 `inverse`；[PR #9603 require deployment name for no-default Azure provider](https://github.com/promptfoo/promptfoo/pull/9603)；[PR #9595 Azure MAI image provider](https://github.com/promptfoo/promptfoo/pull/9595) `azure:image:MAI-Image-2.5` 走 `…/mai/v1/images/generations`（**非 Azure OpenAI 路径，无 `api-version` param**）；[PR #9612/#9610 on-prem report server auto-share + cloud headers preserve](https://github.com/promptfoo/promptfoo/pull/9612)。**5 个反常识**：(a) OSS 供应链攻击恢复期 **> 24 天** 常见（guardrails-ai 0.10.1 投毒 5/11 → 6/6 仍 quarantined 26 天）；(b) **`validate()` 调 lambda 是"无伤大雅"假象**，presidio #2025 实证"探针调用破坏 stateful lambda 真实状态"是 P0 —— 单测 OK、prod 错；(c) **0.10.0 vs 0.10.1 是 single-commit 投毒**，5/11 同窗口 TanStack 42 包 + guardrails-ai 1 包 = **单点入侵导致多包同时投毒**；(d) **garak `digest.technique_intent_matrix` 把"technique × intent"投影作为 first-class** —— 现行 AIGW dashboard 几乎都是"按 probe type"维度切，**intent 维度**（attacker 想 exfil 什么）缺失是 AIGW 可观察性 +1 的硬机会；(e) **promptfoo A2A provider 与 MCP provider 在同框架** —— "tool surface"作为攻击面已经成为 AIGW 通用抽象。**AIGW 5 条新硬要求 → 累加 108 条**：(G-1) **OSS PyPI 供应链攻击缓解（必做）** —— `pip install --require-hashes` + lock + `pip-audit` CI + 验 `module.__file__` 包含 git tag SHA（投毒 wheel 拒收）+ dep 中含 guardrails-ai/presidio/garak/promptfoo/LiteLLM/OpenAI/Anthropic/langchain/Cisco AI Defense/Lakera 任何 OSS 的 prod 部署必须 "PyPI quarantined → git tag fallback" runbook；(G-2) **`validate()` 不调 lambda**（presidio #2025 范式）—— 落地：LiteLLM `proxy/callbacks/` user callback / Higress wasm filter user script / LangChain middleware；(G-3) **Saved Cloud Token 严格 origin allowlist**（**PR #9597 范式**）—— `URL.origin` 与 `cloudConfig.getApiHost()` 比对，**显式不 attach to** look-alike/different port/http-vs-https/unparseable host，**显式 attach to** exact match（incl. port-bearing on-prem），**不要 override caller-supplied `Authorization`**；(G-4) **多模态 grader / recognizer 默认 fail-closed + 显式 opt-in** —— 范式：a) `presidio.recognizers.filter.countries=[]` 显式 = locale-agnostic only，b) Higress wasm `require_explicit_allow` 拉白名单，c) LiteLLM `safe_merge_extra_body` 已落地（继续），d) promptfoo `PROMPTFOO_ALLOW_GRADING_IMAGE_PRIVATE_URLS=true` 显式 env var opt-in + grader metadata **不存 image bytes**；(G-5) **`digest.technique_intent_matrix` 投影作为 red-team 评估 first-class** —— AIGW 报告层加 (a) per-eval intents map `{passed, total_evaluated, nones}` + (b) 顶层 technique×intent 交叉表 digest，前端"按 attack technique" + "按 attacker intent"独立视图，新 probe 加 `demon:*` tag 自动落到正确 cell。详见 [report](reports/2026-06-06-1858-aigw-guardrails-supply-chain-zero-retention.md)。

- 15:54 · `mcp-gateway` · round 24 — **MCP 协议演进 / 静态 API key / OAuth / 审计** 专题。聚焦过去 1.5h MCP spec + 6 SDK 同步硬化集锦：(1) **4 条主信号**：[#2127 SEP-2127 Server Cards](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2127) (dsp-ant, 6/6 01:24 UTC, **+837**) `/.well-known/mcp/server-card.json` + `mcp://server-card.json` 预连接发现 capability/transport/auth/primitives，**与 [#2742](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2742) (auth 块) 强耦合** —— host `initialize` 之前拿到 `none`/`optional`/`required` posture + OAuth 注册 (dynamic/CIMD/static) + client cred type + OOB AS endpoint 防 IdP Mix-Up；[#2867 Scoped Authorization Receipt](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2867) (dinpd, 6/5 19:52, **+100**) tool call receipt 拆 **transport / runtime / provider** 三层 + `replay` / `scope-drift` / `retry-suppression` 三条 acceptance criteria（与 langfuse 5 张新表 / Vaara `vaara.attestation.decision` 对齐，DFIR 阶段必须能回答"谁授权了 side effect"）；[#2809 ATSA](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2809) (metereconsulting, 6/2, **+848**) well-known URI 发**离线签名** clearance assertion，host 用本地 pin 的 trust root 验证后**才 dispatch tool**，**MCP 第一个"server identity ≠ tool authorization"分开硬化的 SEP**，对应 NIST SP 800-53 AC-3/4/6 + AU-2/9 合规；[#2828 Server-Side Signed Execution Record](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/2828) (vaaraio, 6/2, **+1326/-45**) SEP-2787/2817 的 server-authoritative 对偶，side-effect 之前签 **decision record** (allow/block/escalate + risk basis)，之后签 **outcome record** (executed/refused/errored + result commitment)，`backLink` 配对，**RFC 8785 (JCS) + detached signature**，**反常识**："A client can claim its intent and its arguments. It cannot credibly attest that a call was allowed, why, or what it returned." —— **server-side 是 audit 唯一可信源**，wire shape 已落 Vaara MCP proxy (v0.42+ ship)。(2) **8 条 auth/security SEP 集中盘点**：SEP-2127 Server Cards / SEP-2742 Auth Decl in Card (tobinsouth, +427) / SEP-1932 DPoP Profile (PieterKas, +124, RFC 9449 sender-constrained token 防 replay) / SEP-1933 Workload Identity Federation (PieterKas, +333, K8s/SPIFFE/SPIRE JWT 直接当 MCP identity) / SEP-2343 Elicitation-Auth 澄清 (pcarleton, +7/-1, H1 phantom task bounty 启发) / SEP-1488 `securitySchemes` in Tool Metadata (OpenAI issue, noauth/oauth2) / SEP-2643 Structured Auth Denials (monmohan, +743, transport-agnostic JSON-RPC envelope + URL-mode elicitation + RAR RFC 9396 hint) / SEP-2809 ATSA / SEP-2828 Server-Side Execution Record / SEP-2848 Async Approval (mcguinness, +1305, `net.openid.authzen/tool-approval` 实验扩展)。**"MCP auth 的 2026 H2 主线不是更好用，是更可信"** —— bearer token + static API key + scope 列表是"够用"，但 **replay / mix-up / phantom task / OOB approval / 决策可审计** 5 件事在企业部署里是 deal-breaker。(3) **`static API key` 在 MCP 治理中的位置**（用户特别要求）：MCP 协议层**不**为 static API key 提供 first-class 支持，auth 默认 OAuth 2.0/2.1 + RFC 8414/9728。实战 4 类合法用例 = **loopback / 本地 daemon** (Claude Desktop ↔ 本地 mcp-proxy, OS 进程隔离 + 127.0.0.1 是隐式 trust boundary, SEP-2742 posture=optional) / **CI / E2E 测试** (csharp-sdk #1855 conformance auth server, 对应 SEP-2343 no-auth fallback) / **Service-to-service 联邦** (内部 cluster mesh, 真正硬要求走 SEP-1933 WIF) / **MCP-over-WebSocket / 私 deploy** (SEP-1288, 应迁 CIMD)。**MCP 治理"姿态"**：RFC 8707 resource indicator (MCP 1614 5/11 提 `resource` 改 OPTIONAL) / SEP-1488 显式列 "More scheme types can be added later... (API keys, mTLS, etc.)" 走"future"路径 / **rust-sdk #883 / python-sdk #2784 DCR `application_type="native"`** 是"消灭 static key 用例"动作（CLI/desktop 不再需要预共享 secret）/ **csharp-sdk #1625 SEP-990 Enterprise Managed Auth** 企业版同样动作。(4) **6 SDK 12+ 项 auth/security 落地**：python-sdk [#2784 DCR `application_type`](https://github.com/modelcontextprotocol/python-sdk/pull/2784) / [#2782 same-origin `Origin`](https://github.com/modelcontextprotocol/python-sdk/pull/2782)（DNS-rebinding/CSRF；**Honest scoping 段**揭示 Go 1.25 stdlib `if origin == "" { return nil }`，**PR 真实收益是 server-side policy 一致性而非"修了具体 bug"**）/ [#2775 transport security troubleshooting guide](https://github.com/modelcontextprotocol/python-sdk/pull/2775) / #2779/#2787 OAuth query params（双发 6/4-6/5）；go-sdk [#969 `ClockSkew` 30-60s](https://github.com/modelcontextprotocol/go-sdk/pull/969)（云 IdP 漂移容忍，与 #971 `AllowMissingExpiration` 配对）/ #975 SEP-2575 client stateless / #982 keepalive failure threshold；rust-sdk #883 DCR `application_type="native"`（SEP-837 修 CLI/desktop 注册被拒）/ #884 deprecate roots/sampling/logging (SEP-2577)；csharp-sdk [#1625 SEP-990 Enterprise Managed Auth](https://github.com/modelcontextprotocol/csharp-sdk/pull/1625)（**企业版主线**）/ #1619 x-mcp-header SEP-2243 / #1600 require `Tool.inputSchema` / #1599 `InitializeMeta`；typescript-sdk #2140/#2161 OAuth discovery 容错（与 SEP-2351 配对）/ #2236 stateless-revision HTTP route / #2235 stateful protocol version constants；java-sdk #970 bump mcp-client-security 0.1.5→0.1.9 / #975 upgrade MCP-security 0.1.11, implement CIMD。(5) **4 家 LLM gateway 含义**：**LiteLLM** — SEP-2742 → host 在 `/.well-known/mcp/server-card.json` 拿到 `auth` 后**直接选 OBO/PKCE Passthrough/M2M**；SEP-2828 → `litellm_MCPServerExecutions` 表；SEP-2809 ATSA → `litellm_MCPServerTrustRoot` 配置；SEP-2643 → `litellm.errors.AuthorizationDenied` envelope。**Higress** — ai-proxy 与 mcp-bridge 合并 `none`/`optional`/`required` auth posture enumeration 配置；SEP-2343 强制 remote server 端做 auth → Higress wasm filter 必加 `elicitation` call 的 auth check。**Kong** — 3.14.0.x ai-mcp-proxy 已落 OAuth2 + Token Exchange + scope→ACL；SEP-2742 出后 Kong `server-card auth 块` 解析 + 动态 auth 路由是 7 月 LTS 合理目标；SEP-1933 WIF 复用 Kong 企业版 IdP 集成。**Envoy AI GW** — QuotaPolicy-aware routing (r22) + SEP-2828 → ext_proc 把 decision + outcome record 透传回 trace；SEP-2809 走 ext_authz `clearance` claim 校验；SEP-2643 → ext_proc error response 走 envelope。(6) **2026-07-28 RC 进展评估**：**11 条已 final SEP** (5/22-6/4 集中, SEP-2350/2351/2352/2468/2484/2549/2567/2575/2577/2596/2663) + **Auth Spec Split** (merged #2858 + #2862 + #2863) 已落定。**7/28 RC 大概率装下 SEP-2127/2448/2343/2643/2742/2822 + 5 条已 final** —— "discovery / auth hardening / execution audit" 是 RC 三大主线；**SEP-2809/2828/1932/1933/2848 多数会延到 2026-Q4**（"signing / WIF / OOB approval" 是协议层 further-out 改造，需更多 review cycle + reference impl）。**AIGW 5 条新硬要求 → 累加 102 条**：(S-21) static API key 4 类用例文档化 + migration path / (S-22) **不要**新增 first-class static API key gateway 端点（等 SEP-1488 协议层定稿；过渡期 audit log 标 `auth_method=static_key`）/ (S-23) `auth_method` 必须在 audit log 五元组（与 #2867 三层对齐）/ (S-24) go-sdk #969 `ClockSkew` 30-60s 配云 IdP / (S-25) **SEP-1933 WIF 优先于 static API key**，SPIFFE JWT validation 在 gateway 终止，static key 只作 fallback。详见 [report](reports/2026-06-06-1554-aigw-mcp-r24.md)。
- 14:13 · `release` · **LiteLLM (r2)** — 单产品发版追踪。聚焦 6/4–6/6 三主线：(1) **版本总览**：v1.88.0-rc.3 (6/5 02:10 UTC) 跟 rc.2 **没有新 commit**（纯 tag 步进，**stable cut 节奏陷入「卡 3 个 rc」**）；v1.87.1 (6/4 22:12) backport 5 项；v1.86.4 (6/4 16:35) backport 6 项；v1.84.5 (6/4 04:11) 长尾维护线。`mateo-berri` 在 1.87.x / 1.86.x 反复做 **#29612 → 1.87.2 / 1.86.5 → #29645 / #29644 revert 回原版** —— **release 自动化需要 patch 阶段 full CI 而非 target branch CI**。(2) **6 条 main 高信号 PR**：[#29809 `fix(ui): persist Tools-tab MCP OAuth token to DB`](https://github.com/BerriAI/litellm/pull/29809) (6/6 05:29) — **MCP OAuth OBO 模式阻断性 bug**，`access_token` / `refresh_token` 落 `litellm_MCPUserCredentials` 表 + 60s 提前刷新（LIT-3579）；[#28917 `feat(mcp): per-server env vars with global + per-user scopes`](https://github.com/BerriAI/litellm/pull/28917) (6/6 03:15) — `${NAME}` 模板 + listing best-effort + `MCPMissingUserEnvVarsError` + 修 duplicate-row 死锁；[#29820 `fix(proxy): drop deleted team BYOK model name`](https://github.com/BerriAI/litellm/pull/29820) (6/6 01:35) — 修 #22594 鬼模型，**BYOK 模型在 `team.models` 里有「两个真理来源」**（`litellm_modeltable` alias 行 vs `team.models` 直接存的 public name）双写清理；[#29806 `refactor(ui): apiClient 收敛`](https://github.com/BerriAI/litellm/pull/29806) (6/6 03:40) — dashboard `networking.tsx` 61 个手卷 `fetch()` → `apiClient.get/post/put/delete/patch`，**byte-for-byte behavior-preserving**；[#29816 `feat(ui): OpenAPI type generation`](https://github.com/BerriAI/litellm/pull/29816) (6/6 00:20) — `schema.d.ts` 从 FastAPI `app.openapi()` 直接生成（10 文件 / +52976 / -4），新增 `Check UI API Types Sync` CI drift gate；[#29713 `feat(litellm): finish models/repository migration`](https://github.com/BerriAI/litellm/pull/29713) — 所有 DB table 移到 `litellm/models/` + repository 层（LIT-3570）。(3) **1.88 rc 周期切 4 fix + 1 回归**：[#29632](https://github.com/BerriAI/litellm/pull/29632) cherry-pick bundle = [#29545 video model router resolve](https://github.com/BerriAI/litellm/pull/29545) / [#29310 org-team keys](https://github.com/BerriAI/litellm/pull/29310) (VERIA-55 回归) / [#29585 Vertex Claude Haiku 4.5 `output_config.effort` 剥除](https://github.com/BerriAI/litellm/pull/29585) / [#29598 passthrough dedup cost callbacks](https://github.com/BerriAI/litellm/pull/29598)；[#29637 + #29612](https://github.com/BerriAI/litellm/pull/29637) UI session token 免 team budget ceiling（GHSA-q775 加固）。(4) **MCP 集中修 OAuth 四联击（6/3-6/6）**：3 OAuth 变体（M2M / PKCE Passthrough / PKCE OBO）× 2 lifecycle（listing vs call）× 2 数据源（sessionStorage vs DB）= 12 出错点，**已识别并修 4 个** = #29732 (diagnose) / [#29714 UI labelling](https://github.com/BerriAI/litellm/pulls/29714) / #28917 template + lifecycle / [#29809 persistence](https://github.com/BerriAI/litellm/pulls/29809)。**「tools 不见」 bug 三角** = UI labelling 错 / token 不持久 / template 解析抛错。(5) **Issue Tracker 高关注**：[#29818 OpenAI Codex CLI `/v1/responses` 不工作](https://github.com/BerriAI/litellm/issues/29818) — 1.87.1 回归，**Codex CLI 走 OpenAI 6/2026 推的下一代 endpoint，LiteLLM HTTP parser 似乎没正确处理**；[#29808 `MidStreamFallbackError` Vertex AI 不重试](https://github.com/BerriAI/litellm/issues/29808)；[#17479 Bedrock cachePoint extraneous key](https://github.com/BerriAI/litellm/issues/17479)（#29822 修法）。(6) **六个上轮没看到的新走向**：UI 治理三件套（#29793 base URL resolver + #29806 apiClient 收敛 + #29816 OpenAPI type generation）= **大代理项目 dashboard 治理标准组合**；MCP OAuth 三角成 LiteLLM 6/3-6/6 新战场；stable cut 流程反复说明 release 自动化需 patch 阶段 full CI；**Cosign 验证流程已稳定**（每个 release 页面都给 commit-pinned + tag-pinned 两种 verify 路径）—— **docker 镜像的供应链安全是 LiteLLM 在 AI GW 项目里目前唯一公开 SLSA 实践**；**Anthropic Claude Code / OpenAI Codex CLI 兼容性成新隐患** —— CLI 默认 payload 含非标准字段（`output_config.effort`、`/v1/responses`）让 proxy 必须能"剥 + 转"是 2026 H2 通用问题；LiteLLM 主推 1.87.x + 1.88.x 双轨，1.87.1 实际是「1.88 没 stable 之前的事实 stable」。**AIGW 6 条硬要求 L-1~L-6 累加到全集 97 条**：(L-1) MCP OAuth 三变体每变体独立走 e2e smoke test，listing path 与 tool-call path 走不同 test suite；(L-2) gateway 必须确保 path-aware parser 正确分流 `/v1/responses`（OpenAI 推的下一代主路径）；(L-3) 删 + 改操作的「双真理来源」schema 必须明确 source of truth（**`team.models` 删除时双写清理**）；(L-4) UI 治理三件套（base URL 单点 / 客户端单点 / 类型单点）做进 PR review checklist；(L-5) release 自动化 patch 阶段必须跑 full CI 而非 target branch CI（#29612 1.87.2 / 1.86.5 被 revert 实证）；(L-6) Docker 镜像 cosign 验证 + commit-pinned verify 命令每个 release 页面都带（**目前 LiteLLM 是唯一公开 SLSA 实践**）。详见 [report](reports/2026-06-06-1413-aigw-litellm-release-r2.md)。
- 13:21 · `arch-benchmark` · round 6 — **LiteLLM 3 件 PR + vLLM 4 主线 + llm-d bench report + Higress/agentgateway/Envoy 各自硬数字**。聚焦过去 6h 上游 PR 描述自报的 throughput / latency / memory 数字（median of 5 passes 或 200k calls 标定）。(1) **LiteLLM 3 件 PR 把"单 worker RPS 上限"提升 25–50%**：[#29782](https://github.com/BerriAI/litellm/pull/29782) orjson for `/v1/messages` 500K token tool-heavy 负载：68.5 → 84.6 RPS（**+23%**）、p50 127 → 102 ms（**−20%**）、orjson 在 1.9 MB body 0.11 ms vs stdlib 2.84 ms（约 25×），residual event-loop freeze 不变（cold-start/inherent 非序列化）；[#29537](https://github.com/BerriAI/litellm/pull/29537) per-request optional import cache：`/chat/completions` conc 1 RPS 190.5 → 233.2（**+22.4%**）/ overhead 5.25 → 4.28 ms（**−18.5%**）/ conc 64 +17–20%，py-spy `phase_span` 8.1% → 0.0%（`seed_request_identity` 6.0% → 0.0%、`_is_exception_related_to_db` 2.3% → 0.0%），**顺带修真 bug**：prisma 缺失时 `_is_exception_related_to_db` 在 exception handler 里 raise `ImportError` 把真实错误吞掉；[#29761](https://github.com/BerriAI/litellm/pull/29761) pydantic Delta streaming path：`Delta(content,role)` 12.50 → 2.67 µs/call（**−79%**）、`Delta(content,role,tool_calls)` 14.08 → 4.39 µs/call（**−69%**），10 次 `__setattr__` + 5 次 `__delattr__`（pydantic `extra='allow'`）→ 一次 `parent init` 后直写 `__pydantic_extra__` + `__pydantic_fields_set__`，**56-case golden snapshot + serialization contract test** 锁 byte-identical（streaming 路径 `exclude_unset` 关键）。**观察**：每 PR 3–5 文件、单一目标、内置 regression test、火焰图可重跑 —— **当一个 proxy 项目把"perf 门槛"做进 PR review checklist 而不只是 release notes 附录，RPS 上限就跟着改**。(2) **vLLM 4 主线**：[#44677](https://github.com/vllm-project/vllm/pull/44677) DBO++ TP all-reduce overlap compute：Qwen3-32B TP=4 prefill TP-AR 约占 **19%** latency、**−10.55% wall / +11.8% throughput**、AR **95.7%** hidden behind compute，DBO = 2-thread-per-rank micro-batching 沿 token 轴分 2 个 `UBatchSlice`、**同一 GPU 不同 CUDA stream**（`compute_stream` vs `comm_stream`），**当 PCIe > NVLink 时 AR 通信成本更高 DBO 收益更大**（PR 明确写）；[#43137](https://github.com/vllm-project/vllm/pull/43137) `per_token_group_quant` 走 register group=128 bypass SMEM：Qwen3-30B-A3B-Thinking-2507-FP8 TP=4 + EP **+4.5% E2E throughput**、baseline output 13849.18 tok/s/peak 14700/TTFT 116.68 ms/TPOT 8.98 ms/p99 ITL 12.85 ms；[#44708](https://github.com/vllm-project/vllm/pull/44708) benchmark 方法学 auto-detect tokenizer mismatch：client transformers 5.9.0 DSv3.2 vs server ROCm aiter 内置 **输入 token 通胀 6×**（baseline input 46359 预期 10000，benchmark 7.22s output 138.59 tok/s total 6563.52 tok/s TTFT 918.93 ms TPOT 15.99 ms —— **全部基于 inflated 46359 input tokens**），修法 probe 端 `/tokenize` 第一个 prompt 不一致时 `WARNING: tokenizer mismatch (server=X, expected=Y), re-aligning prompts`，**生产侧"tokenizer 漂移"也是成本归因不准根因之一**（与 vllm-sr PRISM cost 层相关）。(3) **llm-d 首份 PR-level bench report + 多 backend 对齐**：[#1705](https://github.com/llm-d/llm-d/pull/1705) predicted-latency-routing benchmarking templates = **第一份"PR 级别、含完整数字表、可复现"的 llm-d bench 报告**：`Qwen/Qwen3-32B` 10 × vLLM decode pods (TP=2, 20 × H100) 131072 context input throughput **+38.3%** output throughput **+40.2%** vs 裸 k8s Service，代码含 `benchmark-templates/run_benchmark.sh`（重启 EPP + per-run PVC + per-concurrency `inference-perf` Job + 早失败检测）+ `bench-job.yaml`（pinned `inference-perf:v0.5.0`）+ 修复 `pluginsConfigFile` 路径 + `prefix-cache-scorer` 缩进 + 声明 `slo-headroom-tier-filter` plugin，**门禁陷阱**：`slo-headroom-tier-filter` 之前没声明 → plugin 静默不加载 → EPP router 配置"看起来对"实际不生效（**配置不报错是 router 项目的最大坑**）；[#1586](https://github.com/llm-d/llm-d/pull/1586)+[#1700](https://github.com/llm-d/llm-d/pull/1700)+[#1661](https://github.com/llm-d/llm-d/pull/1661) WVA 4 类流量（prefill/decode/symmetrical/**bursty** 与生产 P99 关系最密切）+ TPU V6 P/D recipe + nightly benchmark 与 NVIDIA 主线 parity，**llm-d 正在把"benchmark = 一篇 PDF" 转为"benchmark = 每晚 regression test"**。(4) **Higress 性能退避**：[#3922](https://github.com/alibaba/higress/pull/3922) MCP filter 200 MiB 重建阈值与 `ai-proxy` 对齐修 plugin OOM + `wasm.envoy.wasm.runtime.v8.active` 漂移；[#3923](https://github.com/alibaba/higress/pull/3923) **删** request-count based 主动重建触发器、**保留** memory-threshold，在 #3920 调查 Envoy RSS 持续增长期间稳态流量下避免 churn，**性能退避标准解法** = **先关多余开关 → 再加唯一触发器 → 监控中观察**。(5) **agentgateway 配置同步可观测 + 出站时序**：[#2061](https://github.com/agentgateway/agentgateway/pull/2061) `agentgateway_config_synchronized` gauge 0/1（`StateManager` 每次 config update 写入）配合 PromQL `==0` for 1m → page，关掉 reload 失败只能从 controller log 推断的盲区，运维 **gauge + agctl log + pprof** 三件套；[#1784](https://github.com/agentgateway/agentgateway/pull/1784) `agentgateway_request_processing_seconds` histogram label 维度 `backend`/`bind`/`gateway`/`listener`/`route`/`route_rule` bucket 50µs → ...，**`MinimalHTTPLabels` 必须裁剪**避免 cardinality 爆炸。(6) **Envoy AI GW token 配额全链路**：[#1869](https://github.com/envoyproxy/ai-gateway/pull/1869) QuotaPolicy CR → Controller / `translator.BuildRateLimitConfigs()` → RateLimitConfig protobuf → `runner.UpdateConfigs()` → xDS snapshot → rate limit service (gRPC) → Extension Server injects filter + actions → Envoy request-time RLS check (无配额 429) → ext_proc extracts token usage → **stream-done action: RLS `HitsAddend = token count`** → rate limit service increments Redis counter by token count，**核心创新**：「token 用量」直接喂回 RLS 当 counter，`QuotaPolicy { rpm: 100, tpm: 1M, monthly_tokens: 10M }` 同一 RLS 同时 enforce —— **不需要单独 budget service**。**5 个反常识**：通信-计算 overlap 收益在 **PCIe > NVLink** / token quota 可以走同一条限流链路 / tokenizer 不一致让 benchmark 通胀 **6×** / 删 request-count 重建触发器是性能优化 / GPU TPU parity 是路由算法在两栈都能落地的必要前提。**AIGW 6 条硬要求 O-1~O-6 累加到全集 91 条**：(O-1) proxy 内部序列化统一走 orjson（或 simdjson）不强制 base dep `try/except` fallback；(O-2) optional import 一次解析 + 永久 cache（sentinel 模式）prisma/OTel 等可选依赖全部走此模式；(O-3) pydantic 大量 `setattr/delattr` 路径改"一次 init + 直写 `__pydantic_extra__`"，**必须有 golden snapshot 锁 byte-equality**；(O-4) benchmark 工具必须 probe server `/tokenize` 自动对齐 client/server tokenizer 否则数字不可信；(O-5) **删** request-count based 主动重建触发器是反模式（Higress 实证），**只保留 memory-threshold**；(O-6) QuotaPolicy 多维（rpm + tpm + monthly_tokens）同一条 RLS 不需 budget service。详见 [report](reports/2026-06-06-1321-aigw-arch-benchmark-r6.md)。


- 12:42 · `observability` · **r3 · semconv-genai 18 open PR 走向 + v3 scores 4 阶段成型 + agent session 持久化 + OTLP/JSON int64 修复**。本轮聚焦过去 6h OTel/Langfuse/OpenLLMetry/openlit 主仓与新仓的具体走向：(1) **OTel semconv-genai 24h 15 commits 全是 doc / dashboard / dependency / MCP context**（最热 #226 Jupyter notebook 替 models + json schema CI 校验、#256 Slack user map、#212 gen_ai.provider.name 描述归一化、#255/#251/#248 PR dashboard 路由优化、#245 requirement-level 句子大小写、#220 6/4 15:48 UTC 合入 = `merge_commit c4503710d3`、#217 gen_ai.request.top_k 转 int + 拆 gen_ai.retrieval.top_k、#219 GenAI conversation id fallback、#235 Renovate 4h 窗口、#216 GenAI span duration）——SIG 把"治理"前置。(2) **新仓 18 个 open PR 关键四条**：[#197](https://github.com/open-telemetry/semantic-conventions-genai/pull/197) 把 `gen_ai.client.token.usage` 拆为 modality×cache×phase 三 counter（`gen_ai.client.inference.tokens`/`input_tokens_by_cache`/`output_tokens_by_phase`）+ 新 enum `gen_ai.token.{modality,cache,phase}`、`gen_ai.token.modality ∈ {text,image,audio,video,document,unknown}`；[#195](https://github.com/open-telemetry/semantic-conventions-genai/pull/195) A2A client/server span + 5 metric（duration/body size/streaming TTFT/SSE count/outcome）+ 6 低基数 attr（`a2a.method.name`/`a2a.protocol.version`/`a2a.task.state`），reference 走 `a2a-sdk==1.0.3`；[#252](https://github.com/open-telemetry/semantic-conventions-genai/pull/252) `invoke_agent.server`（**SERVER kind**，client.address/port opt-in，与 `invoke_agent.client`/`invoke_agent.internal` 三件套配齐——port of [semantic-conventions#3473](https://github.com/open-telemetry/semantic-conventions/pull/3473)）；[#250](https://github.com/open-telemetry/semantic-conventions-genai/pull/250) `gen_ai.agent.invocation.id`（run-level，**vs `gen_ai.response.id` completion-level**——reference 标 Bedrock Agents `ResponseMetadata.RequestId`/OpenAI Assistants `run.id`/Azure AI Foundry `response.id`，**Capture gap 明写：OpenAI Agents SDK 无独立 run ID**）。(3) **langfuse v3.178.0 之后 main 6/4-6/5 4 大产品方向**：[v3 scores API 4 阶段 40 分钟连发](https://github.com/langfuse/langfuse/pulls?q=is%3Apr+author%3Abezbac+scores+Phase)（LFE-9539：[#13993 Phase 1 polymorphic value flag-gated](https://github.com/langfuse/langfuse/pulls/13993) → [#13996 Phase 2 cursor tuple `(timestamp,id)` base64url](https://github.com/langfuse/langfuse/pulls/13996) → [#14001 Phase 3 field groups](https://github.com/langfuse/langfuse/pulls/14001) → [#14005 Phase 4 filter params](https://github.com/langfuse/langfuse/pulls/14005)——`meta.cursor` 存在即有下一页、`fetch limit+1` 决定是否发 cursor、**不再 COUNT**）；[#13720 agent session 服务端持久化](https://github.com/langfuse/langfuse/pulls/13720) 5 张新表（conversations/messages/runs/confirmation_requests/credential_leases，cascade delete 正确，`providerSessionId` 只服务端）+ 流式 handler 前 persist 用户消息 + run record 流结束 fire-and-forget 更新 + 前端 `syncMessages` 全量 persist + retry-on-invalid-session——**"无状态 pod → DB-backed agent"**；[#14047 OTLP/JSON int64 解码修复](https://github.com/langfuse/langfuse/pulls/14047)——string-encoded int64 `"7"` 之前落 `high * 2^32 + low` `high` undefined → NaN → 被 ingestion `invalid_union` 拒 → **span 静默丢弃**，受影响 OTLP/JSON exporter = OpenTelemetry PHP SDK + 所有自实现；[#14018 agent 框架切换](https://github.com/langfuse/langfuse/pulls/14018) ClaudeAgentAdapter → MastraAgent（`@ag-ui/mastra`+`@mastra/core/agent`+`@mastra/mcp`），**conversation continuity 不再靠 pod-local session ID**——每次请求 server 从 DB 重建历史注入 agent input。(4) **langfuse 运维**：[#14035 deletion mask cleaner](https://github.com/langfuse/langfuse/pulls/14035) `DeletedMaskCleaner` 周期跑 `ALTER TABLE ... APPLY DELETED MASK IN PARTITION` 物理回收 ClickHouse tombstones；[#13980](https://github.com/langfuse/langfuse/pulls/13980) `auditLogs:read` entitlement 强制 batch export；[#14032](https://github.com/langfuse/langfuse/pulls/14032) blob-storage export source visibility 解耦 V4 beta toggle；[#14051](https://github.com/langfuse/langfuse/pulls/14051) Bedrock 默认 `use haiku 4.5`。(5) **OpenLLMetry 0.61.0**（5/31 4643b882 / 64 files / +93/-63）发版后 main 5 天**无新 commit**——0.60→0.61 间隔 42 天，下一发 0.62 大概率 7 月初；本版 13 fix（langchain/anthropic/groq/mistralai/bedrock/ollama/sagemaker/together 7 SDK 一次性补 ERROR status #4101、openai-agents GenAI semconv #3837 + cache_read/reasoning_tokens #4130、chromadb per-doc result events #4105、mcp error.type #4103、anthropic streaming token usage #3949/#3976、evaluator PII reason #4026、SDK dual exporter+processor warn #4137、Pydantic .json() 弃用 #4098）。(6) **openlit 0.0.5→0.0.6 五天双发**（6/2→6/3，#1225 libc6-dev-arm64-cross + #1224 CGO + #1221 buildx 4.1.0 + #1213 eBPF bindings）——**arm64 native build + CGO + eBPF bindings 补齐**，`otel-gpu-collector` 进入可贡献 + 可交叉编译阶段；6/3–6/6 主仓无新 commit 转入 1.21.x 收尾期。**AIGW 6 条硬要求 O-1~O-6 累加到全集 86 条**：(O-1) gateway `usage` 字段按 `{modality, cache, phase}` 三正交维度填充对齐 PR #197 三 counter；(O-2) 自生成 `gen_ai.agent.invocation.id`（UUIDv7）作 run-level 主键**不等 SDK**（OpenAI Agents capture gap）；(O-3) MCP trace context 注入 W3C propagator + `_meta` 字段（PR #220 已 merge）；(O-4) A2A 调用方走 `invoke_agent.client`、server 侧走 `invoke_agent.server`（PR #195+#252）；(O-5) langfuse ingestion 升级解析 OTLP/JSON string-encoded int64；(O-6) langfuse v3 scores API 在 flag-gated 阶段不开、cursor 分页用 `meta.cursor` 存在即有下一页。详见 [report](reports/2026-06-06-1242-aigw-observability-r3.md)。

- 05:30 · `observability` · **GenAI SemConv 5/5 独立成仓 + token 维度精细化**。聚焦 OpenTelemetry GenAI semconv 治理变化与上游落地。(1) **5/5 16:12 UTC PR#3696** lmokova 合入 `semantic-conventions` → 迁入独立 [`semantic-conventions-genai`](https://github.com/open-telemetry/semantic-conventions-genai) 仓库（52 ★）。[opentelemetry-python-contrib PR#4611](https://github.com/open-telemetry/opentelemetry-python-contrib/commit/a341399)（5/22）一次性给 9 个 `instrumentation-genai/` 子包 README 加"contrib notice"指向新库；主仓 commits 显示 6/4–6/5 一周 12 commit 全是 documentation/dashboard/MCP context 校准——SIG 把"治理"前置。(2) 5 月合入六大 semconv：PR#3624（cache read/creation/uncached + reasoning bool，可加性，**禁用 total+子集双发**）、#3565（workflow duration）、#3673（document modality + byte_size + StrippedPart，BFSI KYC）、#3233（`apply_guardrail` span + finding event + `gen_ai.security.decision.type`）、#3250（memory CRUD 五 span）、#2713/2713-related（gen_ai.task.* 任务语义，**已被 reject**，仍走 `invoke_workflow`）。(3) 6 月新仓 18 个 open PR 关键四条：[#197](https://github.com/open-telemetry/semantic-conventions-genai/pull/197) 把 `gen_ai.client.token.usage` 拆为 modality×cache×phase 三 counter（`modality ∈ {text,image,audio,video,document,unknown}`）、[#195](https://github.com/open-telemetry/semantic-conventions-genai/pull/195) A2A client/server span + streaming time-to-first-event、[#252](https://github.com/open-telemetry/semantic-conventions-genai/pull/252) `invoke_agent.server`（SERVER kind，与客户端对偶，给远端 agent runtime）、[#220](https://github.com/open-telemetry/semantic-conventions-genai/pull/220) MCP context propagation 文字定稿（推荐 W3C propagator + `_meta` DNS 前缀豁免）。(4) **Langfuse v3.178.0**（6/2）：[PR#13747](https://github.com/langfuse/langfuse/pull/13747) `Connect in-app agent to langfuse MCP`——trace 查询从被动 dashboard 推向 agent surface；[PR#13980](https://github.com/langfuse/langfuse/pull/13980) auditLogs batch export 强制 entitlement；[PR#13572](https://github.com/langfuse/langfuse/pull/13572) 5/14 把 OpenInference `prompt_details.cache_read/cache_write` 加白名单归一化进 `input_cached_tokens` / `input_cache_creation`；[PR#13716](https://github.com/langfuse/langfuse/pull/13716) 5/21 blob export pricing gate **从按 usage 算转按 model group 锁价**。(5) **OpenLLMetry 0.61.0**（5/31）30+ 修复：openai-agents emit `cache_read.input_tokens`+`reasoning_tokens`(#4130)、#4101 一次性给 9 个 SDK（langchain/anthropic/groq/mistralai/bedrock/ollama/sagemaker/together）补 ERROR status 记录、#3837 openai-agents 切 GenAI semconv、#4103 MCP 协议层 error 第一次走 OTel `error.type`、#4137 SDK 双配 exporter+processor 启动 warn。(6) **OpenLIT 1.21.0**（5/27）主题"闭环"：PR#1185 **offline evals 移除 LLM based**（eval 不再引入 cost）、#1187 guardrails、#1192 remote agent lifecycle、#1200 telemetry views、#1205 Otter trace 深链、#1202 用 trace 反推 prompt 改进。[otel-gpu-collector 0.0.6](https://github.com/openlit/openlit/releases/tag/otel-gpu-collector-0.0.6)（6/3）5 天内 0.0.5→0.0.6，arm64 native build + CGO + eBPF bindings——**GPU 端 OTel 化**。四条 AIGW 硬要求：token metric 走 counter + attribute 带 `{type,cache,phase,modality}`；OpenInference 命名空间 ingestion 归一化；MCP trace context 注入 `_meta` 字段；A2A SDK 先 emit 私有 attribute 等 PR#195 合入。详见 [report](reports/2026-06-06-0530-aigw-observability-genai-semconv.md)。
- 04:50 · `guardrails` · **生产 SLO / 失败模式 / Benchmark 数字**（与 04:10 middleware 互补）。(1) 6/4–6/5 **anthropics/claude-code 一日 5 条 AUP false-positive**——#65699 biomedical Opus 4.8 升 4.7 不再现（**模型层行为回归**）、#65633 gzip decoder perf 自有文件被拦、#65592 颅内出血 CT 分类（EU MDR IIa 合法医疗器械软件）被拦、#65574 cyber safeguard 误拦致**整次 agentic run ~1,000,000 token 计费且无恢复路径**、#65407 Playwright E2E "successful tool result 之后立刻"被拦。模式：**post-tool hook 是新战场、token 损失不可恢复、模型升级即 SLO 风险源**。给 gateway 4 条硬需求：被拦要可观察（`request_id`+`classifier`+`confidence`）、dry-run/audit 模式必须存在、session-level 熔断、hit-reason fingerprint taxonomy。(2) **6/5 17:24 UTC NVIDIA garak v0.15.1**：PR #1504 新增 **ProPILE PII 探针**（Twin/Triplet/Quadruplet/Unstructured，arXiv:2307.01881），默认 `active=False` + `tier=INFORMATIONAL`，数据集 Enron ~50 条；与 `leakreplay` 互补（"已知一条 PII 推断其他 PII" vs "训练集逐字回放"）。同期 27 PR 修 `data_path` 路径遍历（#1781）、删 `maxrecall` evaluator（#1732，强制走 `z_rating` defcon）。(3) **6/4 19:37 UTC guardrails-ai v0.10.2**（CVE-2026-45758 / GHSA-xmpw-2vmm-p4p6）— 5/11 18:00 PT 投毒、5/12 advisory、6/4 重发，**24 天 OSS 供应链响应差**。8 条加固：全 org 凭据轮换、Ray cluster 下线、强制 `Require signed commits` 整 org。**与 TanStack CVE-2026-45321 / GHSA-g7cv-rxg3-hmpx（5/11 22:40 UTC 42 个 @tanstack/* 包 credential exfil）同波攻击者**。(4) **6/4 20:44→21:14 UTC ATR v3.1.0→v3.1.1** 30 分钟双发：462 规则、Tier-2 semantic detection opt-in、`scan-with-judge.mts` 两阶段（regex 10% → +semantic → **95% combined recall**、benign 0/15 FP，n=35 自评 + **shipped harness** 可重跑）；v3.1.1 修 **ATR-2026-00001 false positive**（"start fresh with a new outline/draft"），要求 "new" 后必须 task-like noun。6/5 18:13 UTC PR #95 `agent-threat-rules/mastra` subpath（zero @mastra/core dep, structural generics）。(5) **Presidio #2000** 合入 `countries` 过滤参数（country 从 module path 推断、case-insensitive、`countries=[]` 只留 locale-agnostic），PII 治理在 locale 维度"一行 API"。(6) 给出 4 条 SLO 警戒线：FP rate > 0.5%、FN per category > 5%、block latency p99 > 150ms（Tier-2 judge）、token loss per FP > 100k 触发 session-level 熔断。详见 [report](reports/2026-06-06-0450-aigw-guardrails-production-slo-failures.md)。
- 04:10 · `guardrails` · **prompt injection 防御在 gateway + agent middleware 层的实战**。聚焦 5/4–6/5 三家上游同时落地的"tool-call 前 / 工具结果后"中间件：(1) LangChain 把 security middleware 矩阵铺齐——#37303 ATR Threat Detection（7 条 high-precision 规则源自 Agent-Threat-Rules 425 条 catalog，97.1% recall on NVIDIA garak）+ #37192 SecretMiddleware（tool-arg 凭据 IDS，9 种 BUILTIN_SECRET_TYPES 含 GH fine-grained PAT / LangSmith / OpenAI project / AWS / JWT，draft 状态）+ #34715 RAG 间接注入硬化（XML `<context>` 包裹 + explicit IGNORE 指令，作者 skyvanguard 5/20 closed）+ #37906 memory write validation hook（OWASP ASI-06 长期中毒）+ #36317 monorepo 顶层 THREAT_MODEL.md。(2) LiteLLM 三件前所未见：#29647 修 provider-handler `extra_body` 注入链（`safe_merge_extra_body` 让 validated key 胜出 + Interactions handler 干掉 `model`/`agent` 互斥注入，guardrail `validation_target` 不再被 client 改写——等于把"scan-vs-send mismatch"护栏补上）；#28249 把 Cisco AI Defense 接进 guardrail_hooks，**`inspection_type=chat|mcp` 双 surface** + MCP native JSON-RPC envelope；#29511 sensitive data sticky routing——**整 session 黏性 on-prem**，guardrail 触发的合规路由 ≠ block。(3) Anthropic cookbook #626 一次性 14 项 CMA-MCP 硬化：MCP `getAgent` 字段过滤、`listEvents` 500 cap、IP 限流 60 req/min、1MB body、容器非 root。(4) 反证：`@tzb1-ai` 在 5/30 把"Safety Report: AI Guardrails Do Not Work — 56-Day Proof (06K Loss)" cross-post 到 6+ 个上游 tracker（openai-python/anthropic-sdk/aider/aws-toolkit-vscode/TabNine/generative-ai-docs），reaction 0、形式 spam、内容**不是噪音**——5 项硬需求（hard gate / persistent violation / auth taxonomy / blast radius / mandatory dry-run）正好对应 OSS 5 月以来在 handler-layer 的 enforcement 落地。详见 [report](reports/2026-06-06-0410-aigw-guardrails-prompt-injection-middleware.md)。
- 02:06 · `agent-gateway` · round 18 — **trace 调试 / 成本归因** 专题。agentgateway 在 6/4–6/5 把 multi-agent 诊断链补齐到"OTel 出口 + Prometheus 按 call kind 切分 + CLI 把 pprof/heap/log 一把抓 + config_synchronized gauge"。三主线：#1784 新增 `OutboundCallKind`（Primary/Policy/Mirror）× `OutboundCallSubtype`（Http/Llm/Mcp/ExtAuthz/ExtProc/Guardrail/RateLimit/Oidc）双维度直方图，配 `MinimalHTTPLabels` 防 cardinality 爆炸；#2094 TTFT howardjohn 明确"在 #1784 路径上加 `gen_ai.server.time_to_first_token`"，#1170 GSoC 草案同期给 llm-d-inference-sim 基准（agentgateway 6.3ms / TTFT 3.0ms vs baseline 3.2ms / 0.95ms）。运维侧：#2061 `config_synchronized` gauge 已合；#2098 agctl 重组加 `proxy log` / `controller log`（追加式 directive）；#2096 agctl pprof 封装待合。周边：#2039 多 AI policy 字段级 merge 已合；#1850 MCP prompts 在 multiplex 模式被忽略（影响 DNS-AID 拓扑）；#2095 WebSocket 大小写 bug 让"成功但立刻死"请求污染成本归因。详见 [report](reports/2026-06-06-0206-aigw-agent-gateway-r18.md)。
- 00:46 · `release` · **Helicone** — 单产品发版追踪。285 天未发新 tag（最近 `v2025.08.21-1`），main 仍在高活跃（5/18 单日 5 PR）。聚焦三件事：(1) 5/14–5/18 AWS Bedrock key 误报致 4 天观测层降级，proxy 没事、无数据丢失、全 credentials 轮换；(2) PR #5665 揭示一行 `console.log` 把 CloudWatch 从 1GB/天拉到 1.5–3.9TB/天，4 月单月 \$22,475；(3) PR #5683 揭示 bifrost 站点 2 个月没成功生产部署（Vercel 静默回退旧构建）。AI Gateway 端点 `ai-gateway.helicone.ai` 当前 405 正常，状态页 99.9999% uptime 18+ 月。详见 [report](reports/2026-06-06-0046-aigw-helicone-release.md)。
- 00:13 · `release` · **Kong** — Kong 3.9.2 (OSS) / 3.14.0.5 (EE) 同步发布同日 (2026-06-04)。重点：3.14.0.0 一次性新增 A2A 协议插件、DeepSeek/Databricks/vLLM/Ollama provider；5 个 3.14.0.x patch 全部命中 ai-proxy-advanced 与 ai-mcp-proxy。MCP 治理（OAuth2 + Token Exchange + scope→ACL）已成企业版主线。详见 [report](reports/2026-06-06-0013-aigw-kong-release.md)。

## 2026-06-05

- 23:34 · `agent-gateway` · round 17 — agent gateway 持续追踪。
- 23:02 · `mcp-sdk-reliability` · MCP SDK 可靠性专题。
- 22:11 · `mcp-spec-ia-refactor` · MCP 规范 IA 重构。
- 21:34 · `release` · **Higress** v2.2.2 — Higress 发版追踪。
- 21:02 / 20:11 · `arch-benchmark` · round 4/3 — 架构对比/性能基准。
- 19:38 / 18:58 / 18:19 · `guardrails` · streaming/trust/runtime defense。
- 17:36 · `tech-deepdive-article` · 70KB 技术深度长文。
- 17:04 / 16:49 / 16:18 / 15:28 · `agent-gateway` / `mcp-2026-07-28-rc`。
- 13:18 · `arch-benchmark` · round 2。
- 12:38 · `observability-industry-reshuffle` · 行业洗牌专题。
- 12:08 / 11:54 / 11:10 · `guardrails` PII / 数据驻留 / 供应链。
- 10:31 · `routing-cost-attribution` · 成本归因。
- 09:50 / 09:13 · `mcp-governance-cluster` / `agent-gateway`。
- 08:34 · `mcp-deploy-arch` · MCP 部署架构。
- 08:01 · `release` · **Envoy AI GW** — Envoy 发版追踪。
- 07:16 · `release` · **Portkey** — Portkey 发版追踪。
- 06:32 · `arch-benchmark` · 架构对比/性能基准 round 1。
- 05:45 / 05:06 · `observability` · 可观测/监控专题。
- 04:25 · `guardrails` · guardrails 专题。
- 03:48 / 03:06 · `routing-cost-security` / `semantic-routing-cost`。
- 02:26 · `agent-gateway` · agent gateway 专题。
- 01:46 / 01:06 · `mcp-gateway-products` / `mcp-gateway`。
- 00:34 · `release` · **LiteLLM** — LiteLLM 发版追踪。
- 08:48 · `mcp-server-cards-telemetry-r20` · **新发现 + 新可观测 + 新会话连续性 + 6 SDK 同步硬化集锦**。聚焦过去 12h MCP 主仓与六大 SDK 的 10+ 项新货：(1) **#2127 Server Cards**（dsp-ant / +837/0/4 files / open）——`/.well-known/mcp/server-card.json` 预连接发现 + `mcp://server-card.json` 资源化，"连接前零往返"完成 capability/transport/auth/primitives 拉取（与上一轮 #2855 `server/discover` cacheScope 互补）。(2) **SEP-2448 Server Execution Telemetry**（savula15 / +567/0/4 / open）——`serverExecutionTelemetry` capability + `tools/call._meta.otel` 主动用 **OTLP `resourceSpans`** 把执行 span 推回 client，与 SEP-414 `traceparent` 形成"请求侧 + 响应侧"对偶；Arcade MCP PR #797 落地参考实现。(3) **SEP-2822 Client Generated Session ID**（javapro108 / +782/-43/4 / open）——`Mcp-Client-Session-Id` header + `_meta`，client 生成，server 可忽略，**stateless 部署的 logical conversation 标识符**。(4) 配套：#2867 scoped auth receipt 三层（transport/runtime/provider）+ 三条 acceptance criteria（replay/scope-drift/retry-suppression）/ #2640 Skills Extension `io.modelcontextprotocol/skills` 注册 / #2419 cache_hint well-known key（plain name 不加前缀，等 #1788 落定）/ #1932 DPoP Profile（RFC 9449 sender-constrained token）/ #2866 ElicitationCompleteNotificationParams 抽取（schema 卫生，所有 notification `_meta` 普适继承）。(5) 6 SDK 12h-7d 硬化集锦：csharp-sdk **#1519 Kestrel `_httpSseWriter` 引用泄漏**（Cursor 探活 22 MiB/disconnect，23 分钟 91→204 MiB → OOMKills；`HandleGetRequestAsync` finally 立刻 null）/ **#1528 摘 `application/json` 的 `charset`**（GitHub Copilot MCP 端点 415）/ #1600 `Tool.InputSchema` JSON-required / #1531 stateless 模式跳 5s prune timer / #1599 `McpClientOptions.InitializeMeta`；python-sdk **#2773 重建 stdio client 关闭路径** + #2788 trio virtual clock deflake + transport in-process 化（#2767/#2765/#2764）；go-sdk **#975 SEP-2575 客户端**（`server/discover` 探针 + 三种 fallback）/ #969 `ClockSkew` 配 `RequireBearerTokenOptions`（云 IdP 漂移）/ #971 `AllowMissingExpiration` / #982 `KeepAliveFailureThreshold`；rust-sdk **#883 DCR `application_type="native"`**（SEP-837 修 CLI/desktop 注册被拒）/ #884 SEP-2577 deprecate Roots/Sampling/Logging / #860 `schema_for_input` validate `type:"object"` + `Parameters<String>` 构造期 panic；typescript-sdk **#2128 `-18,280/+423/73 files` 拆 `experimental.tasks.*`**（SEP-2663）/ #2230 server 暴露 `getNegotiatedProtocolVersion()`（client 端早就有）；java-sdk #995 修 transport 关闭时丢错 / #993 URL elicitation SEP-1036。10 条 AIGW 硬要求：`Mcp-Client-Session-Id` 透传 + 注入 W3C tracecontext / Server Cards 自实现 / `_meta.otel` decode 合入 OTel collector / 拒转发 `application/json; charset=utf-8` / DPoP 验签在 gateway 终止 / `cache_hint` 协议层信号驱动 cache 写入策略 / scoped auth receipt 三层分类 / `X-Time-Skew-Tolerance` 30-60s 配云 IdP / `KEEPALIVE_FAILURE_THRESHOLD` 默认 1 可调到 3-5 / DCR `application_type="native"` 默认。**注意：GitHub 推送 401 (Bad credentials)，token 已 revoke，本地已存，等下次拿到新 token 后补推。** 详见 [report](reports/2026-06-06-0848-aigw-mcp-server-cards-telemetry-r20.md)。

- 10:38 · `semantic-routing-r2` · 工业路由器 0.3 时代「合法性 + 冲突解决 + 缓存分层」专题。聚焦过去 3 天语义路由/成本优化 5 大硬信号：(1) **vllm-sr v0.3.0 (2026-06-05 12:08 UTC)** —— 单 release 合并 70+ PR，6 个 GHCR 镜像 + Helm 0.3.0 同步 tag；(a) **PRISM 153-key 合法性层**（`#1563` Mossaab-s，doc only + 白皮书 PDF）—— QUALIFICATION async auto-discovery / CLASSIFICATION in-process `candle-binding` embedding / EXECUTION 沿用 `req_filter_jailbreak` 模式 + 153-Registry in-memory empirical scoring + Re-route loop 不动 `selectModelFromCandidates`（向后兼容）；(b) **entropy-based multi-category domain matching**（`#1497` noalimoy，06-03）—— ModernBERT 给概率 → Shannon 熵 → 低熵 top-1 / 高熵多 category（AND 跨域），BERT-base / mmBERT-32K 自动 fallback，新增 `/api/v1/eval.signal_confidences` 字段，附 `computer_science` vs `computer science` MMLU 标签对齐 bugfix，6 Ginkgo spec 全覆盖，`#1572 UseModernBERT` 在 `#1532` 落地后被 `#1561` revert（`#1574` 修 candle FFI softmax，下一轮再合）；(c) **DSL 表达力**（`#1588` rootfs）—— `SIGNAL_GROUP`（同类信号打包）+ `TEST 块`（DSL 内部单测）+ `TIER routing`（0/1/2/3 tier: 白名单/默认/降级/fallback）+ 冲突检测（规则互斥时解析期报错，不运行时随机挑）；(d) **conflict-free routing workstream**（`#1620` Xunzhuo，06-05）—— 重命名 post-signal 路由契约为 `routing.projections`，新增 `projections.scores` + `projections.mappings`，decisions 可用 `type: projection` 消费，maintained `balance` recipe 重构为 learned + heuristic + partitioned 三策略，5 道门禁 `dashboard-check` / `agent-lint` / `agent-ci-gate` / `agent-feature-gate` / `agent-validate`；(e) **Redis hot cache layer**（`#1423` liavweiss，06-04）—— Milvus 之前叠 Redis，read-through + per-user 失效，key = `user_id + query + project_id + limit + threshold + types`，TTL + key prefix 可配，加 hit/miss/latency 指标，「hot query 不再走向量库」（Redis < 1ms vs Milvus 5-30ms）= demo 与 production 的硬分水岭；(f) **隐私路由配方**（`#1635` Xunzhuo，06-05）—— `deploy/recipes/privacy/` 4 文件（YAML + DSL + probe manifest + README），隐私敏感 → 本地 lane，非敏感 → frontier lane，16/16 probes pass（2026-03-23），对应「敏感数据不出域」硬要求，部署可照搬 4 文件；(g) **ext_proc 性能**—— `#1585` `sjson` 替代 `encoding/json`（request body rewrite），`#1614` `gjson` 替代（response body 提取），p99 latency 直接受益；(h) 配套：`#1502` skip cache for personalized / `#1558` per-decision cache opt-out / `#1627` OpenClaw VSR install bridge / `#1583` 拆 `vllm-sr-sim v0.1.0` 独立发版。(2) **Envoy AI GW #1869 (06-04)** —— QuotaPolicy-aware routing: User creates QuotaPolicy CR → Controller → `translator.BuildRateLimitConfigs()` → RateLimitConfig protobuf → `runner.UpdateConfigs()` → xDS snapshot 推 RLS（Redis）→ Extension Server 注入 filter + actions → Envoy request-time 限流（无配额 429）→ ext_proc 抽 token usage → stream-done `HitsAddend = token count` 推回 RLS → Redis counter 累加，「token 用量」直接喂回 RLS 当 counter，月度「1000 万 token」按量 quota 与 RPM 限流同一个 RLS 不需要单独 budget service；同期 #2023 /v1/audio/* 端点、#2122 Azure OpenAI Responses API、#2052/#2144 OAuth 2.0 Token Exchange + MCPBackend CRD proposal。(3) **LiteLLM v1.87.1 (06-04) + 1.88.0-rc.3 (06-05)** —— 1.87.1 = 5 项 backport（cut by `mateo-berri`），**#29612 session-token budget-ceiling exemption** 稳定版出现在 1.87.x（1.87.2 短暂后 revert），企业场景：team 月度 budget 100M 用完但单 session 因流式 tool-call 长链路需继续执行不被打断 → 该 session 通过 exemption 通过 budget check；06-05~06-06 commits: #29820 修 team BYOK model 悬挂、#29714 MCP playground auth 改 `oauth2` mode、#28370 `you_com` search provider、#29779 cohere v2 chat 支持 `max_completion_tokens`；1.88.0-rc.3 = 3 个 rc，等 stable cut。(4) **LMCache 8 commit / 3 天 (06-05~06-06)** —— `#3521` [Hybrid allocator] 不同 group 不同 block size（异构 GPU 池 / 异构 KV layout 的核心 PR，LMCache 同时管 H100 + L40S / 不同 head 配置的 KV 池）+ `#3528` server_bench `--mode cpu` + `--transfer-mode`（CPU↔GPU transfer benchmark）+ `#3531` create_cache_context factory 重构 mp 路径（06-06）+ `#3524` non-GPU path transfer timing logs（与 CUDA path 对齐）+ `#3507` c_ops vs python_ops_fallback parity test（production 切 C-ops 的门禁）+ `#3522` coordinator CLI + mp server registration（多 server 协调），LMCache 正在从「vLLM 内部 KV-cache 复用」长成「跨 vLLM / SGLang / TensorRT-LLM 的 KV-cache 路由器」。(5) **ModelEngine-Group/unified-cache-management（华为，star 286）** —— `#994` 恢复 Ascend PC e2e gate（昇腾 NPU KV-cache 端到端测试门禁恢复，「华为 + 昇腾」进入商用窗口）+ `#960` layerwise & pipeline store metrics（06-01）+ `#983/#986` 修 FAWA HMA dump handling（05-30，Flash-Attention-Window-Aware + Heterogeneous Memory Access）+ `#985` 优化 MLA TP dump balance（05-26，Multi-head Latent Attention + TP）。Portkey 自 1.15.2 之后 5 月无主线动作（5 个最近 commit 全是 05-19~05-25 的 auth 修补），主线可能从「gateway-first」转向「observability-first」或企业版 BaaS；Helicone 05-18 修 bifrost build / Supabase Node 20 / AWS 事件 banner 撤掉；Kong 3.9.2（06-04）发版主版本线无新东西。**核心信号 3 表格**：「路由器 0.3 时代」三件套 —— 模型选型 legitimacy/compliance/audit（PRISM 153-key）+ 规则表达 SIGNAL_GROUP/TIER/冲突检测 + 缓存分层 Milvus 冷 + Redis 热 + per-decision opt-out + 隐私分级 敏感→本地lane/非敏感→frontierlane + 配额感知 session 豁免 + token 实时回写 RLS + 域判定 entropy 多 category。「路由器与 LLM 推理栈」边界模糊化 —— vllm-sr（带 LLM 推理能力的 sidecar）+ LMCache（跨推理引擎的 KV-cache 路由器）+ Envoy AI GW（Service Mesh 一等工作负载），三层都向「router + classifier + cache + policy」四件套收敛，未来 12 个月最可能看到「vllm-sr + LMCache + Envoy」三选一打包（类似「Istio + Envoy + Prometheus」当年）。AIGW 落点新增 5 条硬要求 → 累加 80 条：**#76 PRISM 153-key 合法性层**（QUALIFICATION/CLASSIFICATION/EXECUTION 三段，in-memory 153-Registry 暂存）/ **#77 entropy-based 多域判定**（ModernBERT + Shannon 熵，AND 跨域）/ **#78 DSL 冲突检测**（`SIGNAL_GROUP`/`TIER`/`TEST` 块 + 解析期冲突检测）/ **#79 Redis hot cache 在 Milvus 之前**（1ms 级，per-user 失效，TTL 可配）/ **#80 个人化响应 skip cache**（user-specific data / tool result / time-sensitive info 强制 opt-out）。详见 [report](reports/2026-06-06-1038-aigw-semantic-routing-r2.md)。

- 17:34 · `semantic-routing-r3` · 「OpenRouter 5 月集中放量 + LiteLLM 1.88 RC budget fix + OTEL v2 + semantic-router CVE」专题。相对 r2（vllm-sr / Envoy / LMCache）切到「商用网关路由器产品矩阵」主轴：(1) **OpenRouter May Release Spotlight（2026-06-01, Nick Livermore）**——月路由 100 万亿 token，5 月一次推四件 = (a) **Model Fusion** 多模型并行 + 合成，API plugin / server tool / chatroom composer 三形态（商用网关一等公民 MoA）；(b) **Pareto Code Router** `min_coding_score` 选最便宜过门槛 coding 模型（cost-quality 从二元到连续）；(c) **Auto router `cost_quality_tradeoff` 0-10 整数**替代旧 binary；(d) **Session-id Provider Stickiness** 同 session 路由到同 provider+同模型，提 provider 端 prompt cache 命中率。配合 Workspace Guardrails / Private Models（Enterprise）/ Per-provider ZDR 一并放出。(2) **OpenRouter Response Caching（2026-04-30 GA, Brian Thomas）**——`X-OpenRouter-Cache: true` 头触发，cache key = `hash(request_body, model, api_key, streaming_mode)`，TTL `X-OpenRouter-Cache-TTL` 1s-24h 默认 5min，主动 bust `X-OpenRouter-Cache-Clear`，响应头 `X-OpenRouter-Cache-Status: HIT|MISS`/`-Age`/`-TTL`。**命中 80-300ms（4ms lookup）/ 零 token 费用**，对照参考：Gemini 2.5 Flash 1.3s、Kimi K2.6 4.6s、GPT-5.5 9.1s。多模态 text/image/audio/document/tool 都支持，base64 参与 hash。作用域 API key 维度，**cache key 含 API key 是有意设计，避免跨 key 数据泄漏**。与 prompt caching 区分：response caching 是 OpenRouter 边缘层跳过 provider 调用、返回完整 response。(3) **LiteLLM 1.88.0-rc.1（2026-05-31）/ rc.2（06-04）/ rc.3（06-05）**：(a) **PR #29273 `fix(router): enforce deployment budgets for dynamically added models`**——补 `add_deployment` / `upsert_deployment`（`/model/new` API）路径 budget 注册到 `RouterBudgetLimiting` + 懒启动 `router_budget_limiting`（fixes LIT-3043 / #25799）；(b) **PR #28909 `feat(otel): typed semconv-aligned OpenTelemetry instrumentation`**（5/30 merge）——`LITELLM_OTEL_V2` env-var 门控，**显式锚定 `set_request_root_span` 到 request root** 解决 pass-through `asyncio.create_task` 关闭 server span 致 orphan 链路 bug，span 树：`SERVER span (FastAPI) → INTERNAL auth (with CLIENT postgres get_key_object) / INTERNAL execute_guardrail / CLIENT chat gpt-4o / CLIENT batch_write_to_db`；(c) **1.87.1 稳定线 / 1.87.2 被回滚**（#29645）——session-token budget-ceiling exemption 在 1.88 RC，**稳定线用户 1.88 GA 前拿不到**；(d) **安全公告近 60 天 5 条与路由/认证强相关**：`GHSA-4xpc-pv4p-pm3w`（2026-05-28, **critical**）Host Header 注入 → authentication bypass / `GHSA-r75f-5x8p-qvmc`（2026-04-20, **critical**）Proxy API key 校验 SQL 注入 / `GHSA-v4p8-mg3p-g94g`（high）MCP stdio test endpoint 认证后 RCE / `GHSA-wxxx-gvqv-xp7p`（high）guardrail 沙箱逃逸 / `GHSA-xqmj-j6mv-4862`（high）`/prompts/test` SSTI。**两条 critical 直接威胁 API key 校验 + 网关鉴权边界**。(4) **aurelio-labs/semantic-router v0.1.15（2026-05-23）修 CVE-2026-42208**（建议升 ≥0.1.15）；v0.1.14（5/18）补 QdrantIndex async 错误 & 缺失方法（PR #663）；v0.1.13（5/14）**QdrantIndex 加 namespace-based 多租户隔离**（PR #661）+ Ollama encoder 参数 `model_name`→`name`（PR #654）+ notebooks 05/07 兼容 v0.1（PR #656）——把 semantic router 从开发玩具推向多租户可用。(5) **Not Diamond 4-5 月连发** 4/23《A Comprehensive Guide to Model Routing》（方法论）、5/6《How to reduce Claude Code costs without sacrificing output quality》（具体案例）、1/20 Prompt Optimization GA、9/25/2024《RoRF: Routing on random forests》——差异化是**轻量 ML 模型路由器**（random forest）而非规则，配合 prompt adaptation 形成双层 cost reduction。(6) **横向对比** LLM Router 形态 7 类：规则 fallback（LiteLLM）/ 学习式（Not Diamond RoRF）/ Pareto 专家编排（Martian）/ 嵌入相似度（aurelio）/ 网关 + Auto（OpenRouter）/ Ensemble（OpenRouter Model Fusion）/ MoE 替代（Martian Airlock）。**AIGW 5 条新硬要求 → 累加 85 条**：(R-1) gateway 必须支持 edge-layer response cache（`X-*-Cache: true` 头 + hash key + TTL + 主动 bust + 可观察响应头）**与** provider 端 prompt cache **互补** 不替代；(R-2) Session-id 路由：同 `session_id` 锁同 provider + 同具体模型，提 multi-turn agentic workflow cache 命中率；(R-3) Auto router `cost_quality_tradeoff` 必为可调连续参数（0-10 整数或浮点），非 binary；(R-4) LiteLLM `LITELLM_OTEL_V2=1` 必须显式打开，**否则路由链路上的 LLM span 父链是错的**（与 PR #28909 ambient-parenting bug 对应）；(R-5) 自部署 LiteLLM 必须升级 ≥ 1.87.1，critical 漏洞（GHSA-4xpc-pv4p-pm3w + GHSA-r75f-5x8p-qvmc）强制升 1.88 GA；用 router budget fix 需 1.88.0-rc.1+。详见 [report](reports/2026-06-06-1734-aigw-semantic-routing-r3.md)。
