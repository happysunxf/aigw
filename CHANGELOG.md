## 2026-06-06 09:25 CST · agent-gateway r20
- 主题：Agent Gateway 专题（第 20 期，09 时段）· multi-agent 编排 / trace 调试 / 成本归因
- 数据来源：agentgateway/agentgateway（v1.3.0-alpha.1 + 14 个新合入 PR）、envoyproxy/ai-gateway v0.6.0、kubernetes-sigs/GAIE v1.5.0、langgraph 1.2.4、crewAI 1.14.6/1.14.7a2
- 重点：envoy-ai-gateway 0.6 把核心 CRD 升 v1beta1，新增 agent-session-id → session.id 默认映射、LLMRequestCostType.ReasoningToken、Response.model 元数据；agentgateway 1.3-alpha 加 dtrace body snapshot、cache_creation_input_tokens 修复、agctl CLI 重构、AGBE a2a first-class backend；GAIE 1.5 推 EPP/BBR 插件框架
- 报告：reports/2026-06-06-0925-aigw-agent-gateway-r20.md (commit 49f9df4)

# AI 网关持续深挖 · CHANGELOG

> 自动化 cron 任务产出记录。每 30 分钟一次，主题按 `local hour % 7` 轮换。
> 报告文件存放在 `reports/`，命名格式 `YYYY-MM-DD-HHMM-aigw-<topic>.md`。

## 2026-06-06

- 11:23 · `guardrails` · **生态快照 R3**（与 04:10 middleware / 04:50 SLO 互补）。聚焦 6/4–6/6 48h 上游硬货：(1) **ATR v3.2.0**（6/5 release）——`Agent-Threat-Rule/agent-threat-rules`（★245，注意 Rule 单数）首次把 Threat Crystallization 结晶规则以 release artifact 形式发布到 PyPI，企业可 pin tag 而非 main HEAD；同日 #104 100% 跨 6 框架（OWASP/NIST/EU AI Act/ISO 42001/SOC 2/Five-Eyes）覆盖 + **CI honesty gate**（claim vs 实际生效规则 SHA 比对）防"声称覆盖 6 框架实际 4 框架"合规通胀；#95 新增 **Mastra 输入处理器 subpath** (`agent-threat-rules/mastra`，**zero `@mastra/core` 依赖**，structural generics 捕获 Mastra Processor 契约，default critical/high 阻断)——ATR 第 5 个 subpath 集成；#94 文档修正 PINT framing（self-built corpus，非 Lakera 官方 PINT）。 (2) **NVIDIA/garak v0.15.1 之上 6 个新 PR**（v0.16 候选主线）——#1846 **special-token injection probe**（5 模板 × 16 special token 双 prompt 对，比对长度/词重叠/refusal 一致性）/ #1843 **base65536 + I-Ching hexagram** 编码绕过（无新运行时依赖，Unicode 非 BMP 平面安全过滤器盲区）/ #1779 **Context Compliance jailbreak**（3 轮假无害 context → 顺势配合有害请求，与 RAG 间接注入同源）/ #1676 **ATR detector 新模块**（9 detector 类 + `garak/data/atr/rules.json` 内置 1,597 regex 来自 293 ATR 生产规则，覆盖 MCP tool poisoning / skill compromise / context exfiltration / excessive autonomy）/ #1845 skip-reason INFO log 解决"用户不知道 detector 为何没跑"/ #1827 修 Py 3.13+ `RuntimeError: reentrant call inside <_io.BufferedWriter>` ——`multiprocessing.Pool` fork 子进程继承父 `FileHandler` fd + 第三方 `__del__` 触发 reentrant flush，fix: 父进程改 `NullHandler`、子进程各起独立 `FileHandler`；#1833 HF Hub 5xx → `pytest.skip()`（维护者意图）/ #1820 修 Wordnet 干净 cache 下 SQLite 索引未建导致的二阶失败 / #1753 补 17 个现代 refusal substring 到 `MitigationBypass` detector（GPT-4o 时代新话术，原 ShareGPT/Vicuna 已 outdated）。 (3) **LiteLLM #28249 Cisco AI Defense 集成**——第 7 个 partner guardrail（前 6：Pillar/PANW Prisma AIRS/Lasso/GraySwan/Hiddenlayer），**首个原生双 surface**：`inspection_type=chat|mcp` 下拉，**MCP 流量走 `/api/v1/inspect/mcp`，body 是顶层 JSON-RPC envelope 本身**（不包 wrapper）——vendor 端可零成本接入无需理解 LiteLLM 内部结构；MCP response 单独成路径 inspect（非 request-only）；auto-discovered via 现有 `guardrail_hooks` loader。vendor SDK [`cisco-ai-defense/ai-defense-python-sdk`](https://github.com/cisco-ai-defense/ai-defense-python-sdk) 6/5 `0ec35ee` (AIFW-24958) 修"connection status unspecified"边界态——强制显式归类（降级模式 + retry 标记），避免 vendor 状态机抖动 → 流量瞬间无 inspect 通过 gateway。 (4) **LiteLLM #29810 协议级 bug**——`cache_control_injection_points` 在 `/v1/responses` 路径上：(a) hook 找 `messages[].role=system` 但 Responses 把 system 放顶层 `instructions` → **静默 no-op**；(b) cache 漏打 + Agents SDK retry → **Claude tool-call 确定性死循环直到 MaxTurns**；与 04:50 报告 anthropics/claude-code #65574 "百万 token 计费"同根——协议层 bug 放大为 token 损失，但 #29810 根因在 gateway 协议翻译。Sibling：#29531 sensitive data sticky routing（closed）/ #29356 InternalServerErrorAllowedFails 静默丢失 / #29737 `auth_v2` flag-gated auth on **authlib + casbin**（WIP，#28249 的 MCP scope 校验复用同 casbin model）/ #29590 Datadog `cost_tag_keys` 传丢失（#28487 consumption 侧 wiring 没接通 production 侧 → FinOps 标签化 cost 永远拿不到）。 (5) **microsoft/presidio 6 月五件套**——#2055 **`max_token_positions = n_words * 2 + 1` 硬上限**（fix #1444，stop-word/punctuation invisible to budget 导致 entity 召回 FP/FN）/ #2041 **`TokenizerBasedTextChunker`**（HF tokenizers 切分，`GLiNERRecognizer` + `HuggingFaceNerRecognizer` 可从 yaml 配）/ #2052 **multilingual NER 模型去重**（8 语言 → 单模型 ≈ 3.2GB 内存节省）/ #2039 **FastAPI server 镜像**（presidio-anonymizer 5 端点：health/anonymize/deanonymize/anonymizers/deanonymizers，async/Python 3.11+ 部署形态）/ #2056 **docker build 文档**（自建 image 哪些 yaml 默认随 published image、`docker build` 怎么 override）。 (6) **meta-llama/PurpleLlama 6/3** `d301f78` 修 AsyncTestInTestCase batch 13/15、5/18→6/2 反复 commit `canary exploit memory corruption` 样本、6/2 `1f18149` `Cap semgrep --jobs` 防 io_uring 资源耗尽——**当前是基础设施硬化而非新威胁本体**，AIGW 短期不要升 main、要稳定 tag。5 条 AIGW 新硬要求 → 累加 90 条：**#86 partner guardrail 的 MCP surface 集成 = 原始 JSON-RPC envelope 透传 + vendor 零解析**（AIGW 自家 MCP gateway 同样要暴露协议层原样 API）/ **#87 协议层 hook 走抽象 schema**（不要硬编码 `messages[].role=system`——`#29810` 已证；hook 返回空集合必须 warn-level log + SLO counter）/ **#88 v3.x release-line pinning**（ATR v3.2.0 TC 结晶规则版本化供应链模型——AIGW 的"威胁本体版本"也走 release tag 锁定，**不要 `pip install @main`**）/ **#89 CI honesty gate**（声称覆盖率 = 实际编入规则，claim drift 直接卡 CI）/ **#90 vendor SDK 边界态 = 生产 SLO 隐藏前置**（Cisco unspecified / garak file lock / LiteLLM no-op 同类——"unspecified 状态不允许 default 分支静默放过，必须降级 + retry 标记"）。详见 [report](reports/2026-06-06-1123-aigw-guardrails-ecosystem-snapshot-r3.md)。
- 08:08 · `mcp-gateway` · **MCP Authorization 规范重构（#2858）+ DCR deprecate + CIMD 升为推荐**。本轮聚焦过去 48h MCP 主仓最重磅的协议层落地：(1) **#2858 Authorization spec split**（localden，6/4 19:16 UTC merge，+951/-895，6 files）——894 行 `authorization.mdx` 拆为 4 子页（`index` 421 / `authorization-server-discovery` 165 / `client-registration` 201 / `security-considerations` 155），sidebar 重排，旧链接失效触发 #2865 修死链。(2) **#2862** 紧跟做"链接归位"（+91/-55，6 files，6/5 10:52 merge）；**#2863** sep-to-spec 一致性清扫（+101/-28，18 files）把 SDK tier 降级规则、deprecation 长期保留 SEP 化。(3) 范式转移核心：DCR（RFC 7591）从可选明确**降级为 Deprecated**（保留向后兼容），CIMD（draft-ietf-oauth-client-id-metadata-document-00）SHOULD 优先；`client_id` 本身就是 client 控制的 HTTPS URL，AS 主动 fetch + 校验 + 不持久化。AS 角色从「持久化 + 颁发」退化为「fetch + 校验」，client_secret 消失，SSRF 风险新引入。(4) `iss` 验证（RFC 9207 / SEP-2468）从 SHOULD 升 MUST 校验；`resource` 参数（RFC 8707）从可选项升 MUST 项——双校验挡住 mix-up + confused-deputy。(5) **SEP-2742**（open，2 评论，6/3 更新）给 `server.json`/Server Cards 加 `auth` 块，5 维度声明（姿态 / 方法菜单 / 注册模式 / 凭据类型 / 带外 AS 端点），其中 OAuth 注册模式选项 `dynamic / client-metadata / static` **正好对应 #2858 优先级**——同期落地的协议层 + 发布层协同。(6) **Inspector v0.22.0**（6/4 12:36 UTC）：PR #1199 npm OIDC trusted publishing（撤长期 token）+ #1270 claude.yml 按 author_association gate（防 Action 投毒）+ #1380 npm audit fix + #1423 URL-mode elicitation。(7) SDK 同步：rust-sdk #883（SEP-837 application_type）+ #884（SEP-2577 deprecate roots/sampling/logging）**同周落地比 spec 主仓更激进**；go-sdk #969 Bearer ClockSkew（防时钟漂移误拒）；csharp-sdk #1600 inputSchema 强校验 + #1599 InitializeMeta（SEP-2575 stateless 适配）；python-sdk #2773 stdio shutdown 重写测试。**对 AI Gateway 四条硬要求**：(a) AS metadata 加 `client_id_metadata_document_supported: true` + fetch/SSRF 黑名单 + DNS 锁 IP；(b) DCR 12 个月宽限期（SEP-2596 政策）；(c) `iss`+`resource` 双 MUST 校验；(d) Server Card 加 auth 块提前声明。详见 [report](reports/2026-06-06-0808-aigw-mcp-auth-spec-split.md)。

- 06:48 · `arch-benchmark` · round 6 — **K8s 原生推理网关专题**：消化 [llm-d#1586](https://github.com/llm-d/llm-d/pull/1586) WVA v0.7.0 真实基准。(1) **WVA benchmark**：H100 + OpenShift，4 类流量 × 3 模型，3-run 平均 — 32B Bursty P99 TTFT **262,441 ms（4.4 分钟塌方）**、错误率 34%；0.6B 同场景 13,376 ms（20× 改善）。(2) **5 个工程结论**：bursty P99 塌方来自 HPA 150 s scale-up window + 103 s pod startup 叠加；WVA saturation-based 决策已够早（KV 75% 触发），物理冷启动是天花板；errors 主要来自 client SDK retry 风暴不是 vLLM OOM；pod startup 32B 103–110 s 决定 warm pool 必须配。(3) **llm-d v0.7.0**（5/12）"UX Change" — 默认切到 standalone 模式（generic proxy）替代 full gateway，CUDA 13.0.2 + driver 580 强制升级，vLLM 0.19.1 锁版不跟 0.22.1 滚动。(4) **GIE v1.5.0**（4/19）4 架构变化：Pluggable Parser Framework、Latency Predictor（事前预测 P99）、BBR body integration（KV cache 命中率优化）、请求插件镜像响应路径。(5) **跨栈对比**：Envoy AI Gateway v0.6 vs GIE+llm-d+WVA — 前者稳（v1.0 GA 6/30）适合 LLM 路由规则+K8s+OTel，后者唯一端到端 vLLM 基准适合自托管+突发+KV 复用；不要混用（CRD+xDS route priority 冲突）。SRE 三硬约束：32B+bursty 必须 warm pool（max 12+1）；禁 client SDK 默认 retry；dashboard 拆 HPA target/current 与 WVA desired 三条线。详见 [report](reports/2026-06-06-0648-aigw-arch-benchmark-inference-gw.md)。

- 05:30 · `observability` · **GenAI SemConv 5/5 独立成仓 + token 维度精细化**。聚焦 OpenTelemetry GenAI semconv 治理变化与上游落地。(1) **5/5 16:12 UTC PR#3696** lmokova 合入 `semantic-conventions` → 迁入独立 [`semantic-conventions-genai`](https://github.com/open-telemetry/semantic-conventions-genai) 仓库（52 ★）。[opentelemetry-python-contrib PR#4611](https://github.com/open-telemetry/opentelemetry-python-contrib/commit/a341399)（5/22）一次性给 9 个 `instrumentation-genai/` 子包 README 加"contrib notice"指向新库；主仓 commits 显示 6/4–6/5 一周 12 commit 全是 documentation/dashboard/MCP context 校准——SIG 把"治理"前置。(2) 5 月合入六大 semconv：PR#3624（cache read/creation/uncached + reasoning bool，可加性，**禁用 total+子集双发**）、#3565（workflow duration）、#3673（document modality + byte_size + StrippedPart，BFSI KYC）、#3233（`apply_guardrail` span + finding event + `gen_ai.security.decision.type`）、#3250（memory CRUD 五 span）、#2713/2713-related（gen_ai.task.* 任务语义，**已被 reject**，仍走 `invoke_workflow`）。(3) 6 月新仓 18 个 open PR 关键四条：[#197](https://github.com/open-telemetry/semantic-conventions-genai/pull/197) 把 `gen_ai.client.token.usage` 拆为 modality×cache×phase 三 counter（`modality ∈ {text,image,audio,video,document,unknown}`）、[#195](https://github.com/open-telemetry/semantic-conventions-genai/pull/195) A2A client/server span + streaming time-to-first-event、[#252](https://github.com/open-telemetry/semantic-conventions-genai/pull/252) `invoke_agent.server`（SERVER kind，与客户端对偶，给远端 agent runtime）、[#220](https://github.com/open-telemetry/semantic-conventions-genai/pull/220) MCP context propagation 文字定稿（推荐 W3C propagator + `_meta` DNS 前缀豁免）。(4) **Langfuse v3.178.0**（6/2）：[PR#13747](https://github.com/langfuse/langfuse/pull/13747) `Connect in-app agent to langfuse MCP`——trace 查询从被动 dashboard 推向 agent surface；[PR#13980](https://github.com/langfuse/langfuse/pull/13980) auditLogs batch export 强制 entitlement；[PR#13572](https://github.com/langfuse/langfuse/pull/13572) 5/14 把 OpenInference `prompt_details.cache_read/cache_write` 加白名单归一化进 `input_cached_tokens` / `input_cache_creation`；[PR#13716](https://github.com/langfuse/langfuse/pull/13716) 5/21 blob export pricing gate **从按 usage 算转按 model group 锁价**。(5) **OpenLLMetry 0.61.0**（5/31）30+ 修复：openai-agents emit `cache_read.input_tokens`+`reasoning_tokens`(#4130)、#4101 一次性给 9 个 SDK（langchain/anthropic/groq/mistralai/bedrock/ollama/sagemaker/together）补 ERROR status 记录、#3837 openai-agents 切 GenAI semconv、#4103 MCP 协议层 error 第一次走 OTel `error.type`、#4137 SDK 双配 exporter+processor 启动 warn。(6) **OpenLIT 1.21.0**（5/27）主题"闭环"：PR#1185 **offline evals 移除 LLM based**（eval 不再引入 cost）、#1187 guardrails、#1192 remote agent lifecycle、#1200 telemetry views、#1205 Otter trace 深链、#1202 用 trace 反推 prompt 改进。[otel-gpu-collector 0.0.6](https://github.com/openlit/openlit/releases/tag/otel-gpu-collector-0.0.6)（6/3）5 天内 0.0.5→0.0.6，arm64 native build + CGO + eBPF bindings——**GPU 端 OTel 化**。四条 AIGW 硬要求：token metric 走 counter + attribute 带 `{type,cache,phase,modality}`；OpenInference 命名空间 ingestion 归一化；MCP trace context 注入 `_meta` 字段；A2A SDK 先 emit 私有 attribute 等 PR#195 合入。详见 [report](reports/2026-06-06-0530-aigw-observability-genai-semconv.md)。
- 04:50 · `guardrails` · **生产 SLO / 失败模式 / Benchmark 数字**（与 04:10 middleware 互补）。(1) 6/4–6/5 **anthropics/claude-code 一日 5 条 AUP false-positive**——#65699 biomedical Opus 4.8 升 4.7 不再现（**模型层行为回归**）、#65633 gzip decoder perf 自有文件被拦、#65592 颅内出血 CT 分类（EU MDR IIa 合法医疗器械软件）被拦、#65574 cyber safeguard 误拦致**整次 agentic run ~1,000,000 token 计费且无恢复路径**、#65407 Playwright E2E "successful tool result 之后立刻"被拦。模式：**post-tool hook 是新战场、token 损失不可恢复、模型升级即 SLO 风险源**。给 gateway 4 条硬需求：被拦要可观察（`request_id`+`classifier`+`confidence`）、dry-run/audit 模式必须存在、session-level 熔断、hit-reason fingerprint taxonomy。(2) **6/5 17:24 UTC NVIDIA garak v0.15.1**：PR #1504 新增 **ProPILE PII 探针**（Twin/Triplet/Quadruplet/Unstructured，arXiv:2307.01881），默认 `active=False` + `tier=INFORMATIONAL`，数据集 Enron ~50 条；与 `leakreplay` 互补（"已知一条 PII 推断其他 PII" vs "训练集逐字回放"）。同期 27 PR 修 `data_path` 路径遍历（#1781）、删 `maxrecall` evaluator（#1732，强制走 `z_rating` defcon）。(3) **6/4 19:37 UTC guardrails-ai v0.10.2**（CVE-2026-45758 / GHSA-xmpw-2vmm-p4p6）— 5/11 18:00 PT 投毒、5/12 advisory、6/4 重发，**24 天 OSS 供应链响应差**。8 条加固：全 org 凭据轮换、Ray cluster 下线、强制 `Require signed commits` 整 org。**与 TanStack CVE-2026-45321 / GHSA-g7cv-rxg3-hmpx（5/11 22:40 UTC 42 个 @tanstack/* 包 credential exfil）同波攻击者**。(4) **6/4 20:44→21:14 UTC ATR v3.1.0→v3.1.1** 30 分钟双发：462 规则、Tier-2 semantic detection opt-in、`scan-with-judge.mts` 两阶段（regex 10% → +semantic → **95% combined recall**、benign 0/15 FP，n=35 自评 + **shipped harness** 可重跑）；v3.1.1 修 **ATR-2026-00001 false positive**（"start fresh with a new outline/draft"），要求 "new" 后必须 task-like noun。6/5 18:13 UTC PR #95 `agent-threat-rules/mastra` subpath（zero @mastra/core dep, structural generics）。(5) **Presidio #2000** 合入 `countries` 过滤参数（country 从 module path 推断、case-insensitive、`countries=[]` 只留 locale-agnostic），PII 治理在 locale 维度"一行 API"。(6) 给出 4 条 SLO 警戒线：FP rate > 0.5%、FN per category > 5%、block latency p99 > 150ms（Tier-2 judge）、token loss per FP > 100k 触发 session-level 熔断。详见 [report](reports/2026-06-06-0450-aigw-guardrails-production-slo-failures.md)。
- 04:10 · `guardrails` · **prompt injection 防御在 gateway + agent middleware 层的实战**。聚焦 5/4–6/5 三家上游同时落地的"tool-call 前 / 工具结果后"中间件：(1) LangChain 把 security middleware 矩阵铺齐——#37303 ATR Threat Detection（7 条 high-precision 规则源自 Agent-Threat-Rules 425 条 catalog，97.1% recall on NVIDIA garak）+ #37192 SecretMiddleware（tool-arg 凭据 IDS，9 种 BUILTIN_SECRET_TYPES 含 GH fine-grained PAT / LangSmith / OpenAI project / AWS / JWT，draft 状态）+ #34715 RAG 间接注入硬化（XML `<context>` 包裹 + explicit IGNORE 指令，作者 skyvanguard 5/20 closed）+ #37906 memory write validation hook（OWASP ASI-06 长期中毒）+ #36317 monorepo 顶层 THREAT_MODEL.md。(2) LiteLLM 三件前所未见：#29647 修 provider-handler `extra_body` 注入链（`safe_merge_extra_body` 让 validated key 胜出 + Interactions handler 干掉 `model`/`agent` 互斥注入，guardrail `validation_target` 不再被 client 改写——等于把"scan-vs-send mismatch"护栏补上）；#28249 把 Cisco AI Defense 接进 guardrail_hooks，**`inspection_type=chat|mcp` 双 surface** + MCP native JSON-RPC envelope；#29511 sensitive data sticky routing——**整 session 黏性 on-prem**，guardrail 触发的合规路由 ≠ block。(3) Anthropic cookbook #626 一次性 14 项 CMA-MCP 硬化：MCP `getAgent` 字段过滤、`listEvents` 500 cap、IP 限流 60 req/min、1MB body、容器非 root。(4) 反证：`@tzb1-ai` 在 5/30 把"Safety Report: AI Guardrails Do Not Work — 56-Day Proof (06K Loss)" cross-post 到 6+ 个上游 tracker（openai-python/anthropic-sdk/aider/aws-toolkit-vscode/TabNine/generative-ai-docs），reaction 0、形式 spam、内容**不是噪音**——5 项硬需求（hard gate / persistent violation / auth taxonomy / blast radius / mandatory dry-run）正好对应 OSS 5 月以来在 handler-layer 的 enforcement 落地。详见 [report](reports/2026-06-06-0410-aigw-guardrails-prompt-injection-middleware.md)。
- 02:06 · `agent-gateway` · round 18 — **trace 调试 / 成本归因** 专题。agentgateway 在 6/4–6/5 把 multi-agent 诊断链补齐到"OTel 出口 + Prometheus 按 call kind 切分 + CLI 把 pprof/heap/log 一把抓 + config_synchronized gauge"。三主线：#1784 新增 `OutboundCallKind`（Primary/Policy/Mirror）× `OutboundCallSubtype`（Http/Llm/Mcp/ExtAuthz/ExtProc/Guardrail/RateLimit/Oidc）双维度直方图，配 `MinimalHTTPLabels` 防 cardinality 爆炸；#2094 TTFT howardjohn 明确"在 #1784 路径上加 `gen_ai.server.time_to_first_token`"，#1170 GSoC 草案同期给 llm-d-inference-sim 基准（agentgateway 6.3ms / TTFT 3.0ms vs baseline 3.2ms / 0.95ms）。运维侧：#2061 `config_synchronized` gauge 已合；#2098 agctl 重组加 `proxy log` / `controller log`（追加式 directive）；#2096 agctl pprof 封装待合。周边：#2039 多 AI policy 字段级 merge 已合；#1850 MCP prompts 在 multiplex 模式被忽略（影响 DNS-AID 拓扑）；#2095 WebSocket 大小写 bug 让"成功但立刻死"请求污染成本归因。详见 [report](reports/2026-06-06-0206-aigw-agent-gateway-r18.md)。
- 00:46 · `release` · **Helicone** — 单产品发版追踪。285 天未发新 tag（最近 `v2025.08.21-1`），main 仍在高活跃（5/18 单日 5 PR）。聚焦三件事：(1) 5/14–5/18 AWS Bedrock key 误报致 4 天观测层降级，proxy 没事、无数据丢失、全 credentials 轮换；(2) PR #5665 揭示一行 `console.log` 把 CloudWatch 从 1GB/天拉到 1.5–3.9TB/天，4 月单月 \$22,475；(3) PR #5683 揭示 bifrost 站点 2 个月没成功生产部署（Vercel 静默回退旧构建）。AI Gateway 端点 `ai-gateway.helicone.ai` 当前 405 正常，状态页 99.9999% uptime 18+ 月。详见 [report](reports/2026-06-06-0046-aigw-helicone-release.md)。
- 00:13 · `release` · **Kong** — Kong 3.9.2 (OSS) / 3.14.0.5 (EE) 同步发布同日 (2026-06-04)。重点：3.14.0.0 一次性新增 A2A 协议插件、DeepSeek/Databricks/vLLM/Ollama provider；5 个 3.14.0.x patch 全部命中 ai-proxy-advanced 与 ai-mcp-proxy。MCP 治理（OAuth2 + Token Exchange + scope→ACL）已成企业版主线。详见 [report](reports/2026-06-06-0013-aigw-kong-release.md)。

- 07:26 · `release` · **OpenRouter** — 单产品发版追踪。OpenRouter 不是 semver 仓，"发版"是平台信号三层叠加：`/api/v1/models` 元数据新模型上架 + `announcements/*` 长文 + `OpenRouterTeam/*` GitHub 仓维护 + 上游 LiteLLM/Portkey 适配 PR。本次 6/4 一天撞 4 块动态：(1) **NVIDIA 一天 3 个新模型**（Nemotron 3.5 Content Safety `:free` 4B 多模态守门员 + Nemotron 3 Ultra 550B/A55B MoE `:free` + 付费版 cache_read 1.5e-7） + Qwen3.7-Plus 6/3（4e-7/1.6e-6、cache_write 5e-7）。近 60 天新模型：qwen 7、anthropic 5、nvidia 4、openai 4、openrouter 3。(2) **2026 年 4 月 43 个新模型 = 年迄今最大月**（与 April Release Spotlight / Workspaces / Agent SDK / Video Gen / Auto Exacto 同档期）。(3) **6 个平台自有产品 SKU** 形态：auto（meta-routed，多模态输入+image 输出）/ fusion（5/12 新，多模型加权融合）/ pareto-code（Artificial Analysis 编码排名）/ owl-alpha（4/28，agentic）/ free（free 池随机）/ bodybuilder（NL→API request）。三个 SKU 切出"路由/质量/成本"三档，**LiteLLM/Portkey/Envoy 都没做**。(4) **Reasoning 抽象**：`reasoning_config` 4 字段（max_tokens / effort / enabled / return_mechanism），426 端点覆盖、118 强制 reasoning、40 支持 reasoning.max_tokens——和 LiteLLM 客户端封装 reasoning 方向**完全同构**。(5) **关键洞**：`openrouter/auto` 路由后真实子模型**没有回填给客户端**，LiteLLM #29562（6/3 open）+ #29710（6/4 open）两个 PR 都在补这洞——OpenRouter 平台层可观测缺口。(6) **生态**：9 个上游仓库 5/15 之后 commit 提到 openrouter；`OpenRouterTeam/openrouter-runner` 9/6/2025 **已 archive**（一句 `archive: everything not saved will be lost`）；`awesome-openrouter` 6/4 单日 12 commit（11 个新 app：Spokenly / Octomind / Roboflow / SoulForge / Agent Swarm / Warden / Space Agent / Stirrup / VT Code / Skales / SillyTavern，`apps.json` 当前 47 个）。(7) **Provider 矩阵**：83 个 provider / 766 endpoint（按 endpoint 展开）/ 344 唯一模型；cache_read 151 / cache_write 45 / web_search 14 / reasoning_effort 4 / image output 7 / audio output 4。详见 [report](reports/2026-06-06-0726-aigw-openrouter-release.md)。
## 2026-06-05

- 23:34 · `agent-gateway` · round 17 — agent gateway 持续追踪。
- 23:02 · `mcp-sdk-reliability` · MCP SDK 可靠性专题。
- 22:11 · `mcp-spec-ia-refactor` · MCP 规范 IA 重构。
- 21:34 · `release` · **Higress** v2.2.2 — Higress 发版追踪。
- 21:02 / 20:11 · `arch-benchmark` · round 4/3 — 架构对比/性能基准。
- 19:38 / 18:58 / 18:19 · `guardrails` · streaming/trust/runtime defense。
- 17:36 · `tech-deepdive-article` · 70KB 技术深度长文。
- 17:04 / 16:49 / 16:18 / 15:28 · `agent-gateway` / `mcp-2026-07-28-rc`。
- 13:18 · `arch-benchmark` · round 2。
- 12:38 · `observability-industry-reshuffle` · 行业洗牌专题。
- 12:08 / 11:54 / 11:10 · `guardrails` PII / 数据驻留 / 供应链。
- 10:31 · `routing-cost-attribution` · 成本归因。
- 09:50 / 09:13 · `mcp-governance-cluster` / `agent-gateway`。
- 08:34 · `mcp-deploy-arch` · MCP 部署架构。
- 08:01 · `release` · **Envoy AI GW** — Envoy 发版追踪。
- 07:16 · `release` · **Portkey** — Portkey 发版追踪。
- 06:32 · `arch-benchmark` · 架构对比/性能基准 round 1。
- 05:45 / 05:06 · `observability` · 可观测/监控专题。
- 04:25 · `guardrails` · guardrails 专题。
- 03:48 / 03:06 · `routing-cost-security` / `semantic-routing-cost`。
- 02:26 · `agent-gateway` · agent gateway 专题。
- 01:46 / 01:06 · `mcp-gateway-products` / `mcp-gateway`。
- 00:34 · `release` · **LiteLLM** — LiteLLM 发版追踪。
